# Failures

Persistent failures that survived the heal loop (3 cycles in `/abap-validate`) and were escalated to the human. Each entry: date, story-group/object, the gate that blocked, and why it could not be auto-healed.
