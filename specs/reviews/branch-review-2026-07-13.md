# Independent whole-branch review — feature/analyser-enrichment (main..HEAD)

Date: 2026-07-13 · HEAD at review: b91ad32 (101 commits ahead of main) · Suite at review: 872/872 green
Method: 8 fresh-context adversarial finders partitioned by subsystem; every MEDIUM+ finding independently
adversarially verified (verifier briefed to REFUTE, default-refuted on ambiguity). 35 agents, 812 tool calls.
Result: **27 confirmed findings, 0 refuted, 15 unverified LOWs** (listed, per no-silent-caps).

## Confirmed findings (ranked)

### F1 [HIGH] Warn-ratchet fails OPEN when diff_changed_lines is missing or malformed — silent delta-0 join the module's own contract forbids
- Subsystem: moderniser-state-ratchet
- File: moderniser/src/state/ratchet.js:70
- Evidence: ratchet.js:70 `delta = warnOnChangedLines(node.diff_changed_lines, evidence.atc_warns);` — warnOnChangedLines(undefined|[]|wrong-shape, warns) builds an empty changed-set and returns 0. Probe: node without diff_changed_lines + evidence containing 2 warns on the rewritten lines -> `PASS, delta 0, atc_warn_delta 0, reasons []`; malformed entries `[{path,rows}]` -> same PASS; `onPass` then establishes the baseline at 0. The module docstring (line 16) claims 'Missing evidence fails CLOSED' and lines 43-46 justify shape-validating warn elements because a mismatched join 'would silently fail-open' — yet only the WARN side of the join is validated (lines 65-68); the DIFF side is not. cli.js:158 actively erases absence: `diff_changed_lines: evidence.diff_changed_lines ?? []`.
- Absence-of-guard proof: Traced the full chain: cli.js cmdVerdict (line 158) is the only production constructor of the gate node and maps absent evidence.diff_changed_lines to []; loop.js renderVerdict passes the node through untouched; no schema validation of the --evidence file exists anywhere (grep for diff_changed_lines across the repo shows only ratchet.js, cli.js, tests, and the design doc). Every gated node has by definition been regenerated, so a genuinely empty diff at the checkpoint can only mean the diff tool errored or the assembler omitted the field — the exact 'tool returns no result => FAIL' case §3.2 5.3 requires to BLOCK. Consequence in both directions: introduced WARNs pass silently now, and the baseline is established at 0 instead of the true delta, which false-BLOCKs later legitimate passes once diff evidence appears (the durable ratchet state is corrupted).
- Proposed fix: Mirror the atc_warns guard for the diff side: in ratchetGate, before the join, `if (!Array.isArray(node.diff_changed_lines)) reasons.push("diff-missing"); else if (!node.diff_changed_lines.every(d => typeof d.file === "string" && Array.isArray(d.lines) && d.lines.every(Number.isInteger))) reasons.push("diff-malformed");` keeping atc_warn_delta at the fail-closed default 1; and in cli.js:158 drop the `?? []` so absence propagates to the gate. Update cli.test.js GREEN_EV to carry an explicit diff_changed_lines array.
- Confidence: CONFIRMED
- Test evidence: 272/272 tests pass. ratchet.test.js:76 ('missing evidence fails CLOSED') enumerates absent atc_p1, non-array warns, and non-finite coverage — but omits the fourth evidence input, the diff. cli.test.js GREEN_EV (line 22) has no diff_changed_lines key at all, so the entire CLI verdict suite exercises and green-lights the fail-open path. The design pseudocode (MODERNISER_DESIGN.md:318) also lacks the guard, but its own parenthetical ('never a silent delta-0 join') and §3.2 5.3 state the fail-closed principle the implementation and tests both miss — either the code or the module's stated contract is wrong; they currently contradict.
- Adversarial verification: SURVIVED — CONFIRMED by direct probe against moderniser/src/state/ratchet.js: node without diff_changed_lines + 2 warns on rewritten lines -> {verdict:PASS, delta:0, atc_warn_delta:0, reasons:[]}; malformed [{path,rows}] entries -> same silent PASS (warn-side equivalents BLOCK with atc-warns-malformed); onPass then writes the durable baseline at 0 instead of the true delta 2, and a later run with real diff evidence false-BLOCKs (warn-delta-regressed:2>0). Guard absence traced end-to-end: cli.js:156 raw JSON.parse of the LLM-evaluator-authored evidence file (no schema validation anywhere), cli.js:158 erases absence via `?? []`, renderVerdict passes through, ratchetGate:65-68 validates only the WARN side of the join, nodeVerdict consumes only the resulting delta-0. Not ratified design (Rule 5): the module docstring line 16 ("Missing evidence fails CLOSED"), design doc line 188 ("tool that returns no 

### F2 [HIGH] applyProgress accepts terminal statuses — unearned GREEN (GAN bypass), permanent dependent starvation, quarantine/mutex skip
- Subsystem: moderniser-sched
- File: moderniser/src/sched/loop.js:108
- Evidence: applyProgress does `let next = setStatus(plan, state, sig, nextStatus)` with no terminal-status rejection; the FSM legally allows GATED→GREEN and any-active→BLOCK/NEEDS_MANUAL_SEAM. Probe (unit): after walking A to GATED, `applyProgress(plan, st, A, 'GREEN')` → "A status: GREEN | verdict_green: undefined; B indegree after A GREEN via progress: 1"; `applyProgress(..., 'BLOCK')` → "deferral_track: []". Probe (production CLI, scratch --state-dir): `progress r1 <sig> GREEN` after GATED → status shows "counts": {"GREEN": 1, "PENDING": 2} with no verdict ever recorded, and the dependent GL's indegree stays 1 forever (run can never complete).
- Absence-of-guard proof: The earned-GREEN guard (`verdict_green[sig] !== true → throw`) and the indegree decrement live ONLY in applyOutcome (loop.js:130-140); the deferral_track record and releaseActivation only in applyOutcome's BLOCK branch. applyProgress → setStatus → assertTransition consults the FSM alone, and the FSM cannot distinguish channels. cli.js cmdProgress (lines 82-91) passes the prose-supplied status string straight through, so the bypass is reachable in the shipped CLI with a one-word slip (`progress` instead of `outcome`). The module docstring claims 'Status moves are FSM-guarded — the loop can never mis-route' and applyProgress's JSDoc says 'non-terminal', but nothing enforces it.
- Proposed fix: In applyProgress, reject terminal statuses before setStatus: `if (["GREEN","BLOCK","PARK","NEEDS_MANUAL_SEAM"].includes(nextStatus)) throw new Error('loop: terminal outcomes must go through applyOutcome')`. Optionally mirror an allowlist of forward statuses in cli.js cmdProgress for defense in depth.
- Confidence: CONFIRMED
- Test evidence: All 272 tests pass. loop.test.js:199-210 ('GREEN is REFUSED without a recorded green verdict') guards ONLY applyOutcome; grep of cli.test.js shows progress is only ever driven with forward statuses (GENERATED..GATED) — no test passes GREEN/BLOCK to applyProgress or `progress`.
- Adversarial verification: SURVIVED — CONFIRMED at HIGH. Mechanism verified by reading the full chain: applyProgress (loop.js:105) -> setStatus (loop.js:217) -> assertTransition/canTransition (node-status.js:68-85); the FSM permits GATED->GREEN (FORWARD map) and any-active->BLOCK/NEEDS_MANUAL_SEAM (line 70) and cannot distinguish channels. The earned-GREEN guard, indegree decrement, deferral_track record, and releaseActivation live ONLY in applyOutcome (loop.js:130-160). Independent probes reproduced the claim exactly: reducer probe — applyProgress GREEN at GATED accepted (verdict_green undefined, dependent indegree stuck at 1, dispatch refused, runComplete false) with NO repair path (applyOutcome GREEN refused, recordVerdict requires GATED, FSM has no GREEN exits — REPLAN is the only recovery); applyProgress BLOCK left deferral_track empty and the activate mutex permanently held (co-transport acquire=false, BLOCK->BLOCK ill

### F3 [HIGH] Re-entry regeneration keeps the stale recorded verdict — outcome GREEN accepted on a regenerated artifact that was never verdicted
- Subsystem: cli-skill-exception
- File: moderniser/src/sched/loop.js:220
- Evidence: Probe (subprocess, scratch dir): walk node to GATED, record green verdict, then `outcome NEEDS_MANUAL_SEAM` → `progress PENDING` → re-dispatch → walk GROUNDED→GENERATED→…→GATED (regenerated artifact, NO verdict) → `outcome GREEN` → ACCEPTED. Final state: status GREEN, cycle 0, verdict_green true. Same holds via PARK→PENDING re-entry. Root: `const retry = to === "GENERATED" && (from === "SYNTAX_OK" || from === "GATED");` — verdict_green is deleted (line 227, comment: "a stale verdict can never bless a REGENERATED artifact") only inside this branch.
- Absence-of-guard proof: Grep proves verdict_green is touched only in initRun, recordVerdict, the applyOutcome GREEN check, and the setStatus retry-void (loop.js:225-228). The re-entry regeneration path (PARK→PENDING or NEEDS_MANUAL_SEAM→PENDING, then GROUNDED→GENERATED) never clears it and nothing in cli.js/cli-io.js compensates. Both re-entry transitions are legal FSM edges (node-status.js FORWARD map + active→NEEDS_MANUAL_SEAM), reachable through documented CLI verbs — the SKILL itself instructs `progress <sig> PENDING` for seam re-entry.
- Proposed fix: In setStatus, void verdict_green on EVERY transition into GENERATED (not only the two retry edges), keeping the cycle increment on the retry edges only: change the void condition to `to === "GENERATED"`. Alternatively clear verdict_green (and the attestation generation stamp, see companion finding) on re-entry to PENDING.
- Confidence: CONFIRMED
- Test evidence: no-test — cli.test.js:302 ("attestation is bound to the ARTIFACT") and loop-level tests cover only the GATED→GENERATED retry edge; no test exercises PARK/NEEDS_MANUAL_SEAM re-entry followed by a re-walk. All 14 cli tests pass without covering this path.
- Adversarial verification: SURVIVED — Confirmed by direct reducer probe (node, scratch dir): after recordVerdict at GATED, both NEEDS_MANUAL_SEAM→PENDING and PARK→PENDING re-entries keep verdict_green=true through a full re-walk (GROUNDED→GENERATED does not match the retry predicate at loop.js:220, so the void at :225-228 never fires), and applyOutcome GREEN is accepted at the second GATED with no verdict for the regenerated artifact — final state GREEN, cycle 0. Grep proves verdict_green is touched nowhere else (cli.js/cli-io.js add no compensation; applyProgress's re-entry branch clears park_register/deferral_track only). Both edges are legal FSM moves (node-status.js:53-54,70) and SKILL.md:74 documents the `progress <sig> PENDING` re-entry verb. Not design intent: loop.js:227/46/58 state the opposite invariant, and the sibling parityAttestation is explicitly run+cycle bound. Tests: 33/33 pass; loop.test.js:224 covers only

### F4 [HIGH] Attestation temporal binding keyed to the retry-cycle counter — defeated by re-entry regeneration and by pre-attestation before any artifact exists
- Subsystem: cli-skill-exception
- File: moderniser/src/cli.js:119
- Evidence: parityAttestation: `if ((latest.decided_cycles?.[sig] ?? -1) !== (state.cycle?.[sig] ?? 0)) return null;`. Probe B: attest gray-band at cycle 0 → verdict green:true; `outcome NEEDS_MANUAL_SEAM` → `progress PENDING` → re-dispatch → re-walk to GATED (cycle still 0) → verdict on the REGENERATED artifact: green:true — the stale attestation applied. Probe C: `escalate PARITY_REVIEW` + `decide ATTEST_EQUIVALENT` while the node is still PENDING (no artifact exists), then generate and walk to GATED → verdict green:true — the decision pre-blesses code the human never saw.
- Absence-of-guard proof: state.cycle increments only at loop.js:224 inside the same retry branch as the verdict void, so re-entry regeneration leaves it unchanged; cmdDecide (cli-escalations.js:59) stamps `state.cycle?.[s] ?? 0` regardless of node status, so a PENDING node with no artifact stamps 0 and matches the later cycle-0 walk. The code comment claims "a regenerated artifact or another run can never inherit an attestation" — the run half holds (probed), the artifact half does not. No content hash or generation counter enters the binding.
- Proposed fix: Bind attestations to a per-sig generation counter incremented on every transition INTO GENERATED (or to an artifact content hash carried in the evaluator's checkpoint), and refuse `decide … ATTEST_EQUIVALENT` for PARITY_REVIEW unless the node is at GATED — a human cannot attest an artifact that does not exist.
- Confidence: CONFIRMED
- Test evidence: cli.test.js:302 asserts the binding for the GATED→GENERATED retry and the cross-run case (both pass, both re-verified by probe D1); it asserts the right thing but only for those paths — the re-entry and pre-attestation paths are untested.
- Adversarial verification: SURVIVED — Both probes reproduce end-to-end against a scratch state dir. Probe B: attest gray-band at cycle 0 (green:true) → outcome NEEDS_MANUAL_SEAM → progress PENDING → re-dispatch → re-walk to GATED leaves state.cycle[sig]=0 (loop.js:220-224 increments only on SYNTAX_OK/GATED→GENERATED; GROUNDED→GENERATED is not a retry), so verdict on the regenerated artifact returned green:true and outcome GREEN was ACCEPTED — verdict_green also survived the detour (void logic loop.js:225-229 sits in the same retry branch). Probe C: decide ATTEST_EQUIVALENT on a still-PENDING node stamped decided_cycles={sig:0} (cli-escalations.js:59 has no status/artifact check, nor do recordDecision/resolveEscalation), and the later cycle-0 walk got green:true. No guard exists anywhere in the chain (no content hash/generation counter; cmdEscalate checks only plan membership), and the skill's ordering (SKILL.md step 4: verdi

### F5 [HIGH] Baseline files gained a second writer but the first writer's contract and the files' _note were never updated — an evaluator-shaped rewrite silently resets every ratchet ceiling to seed-∞ (fail-open), and the design's claimed 'node lock' does not exist
- Subsystem: cross-cutting-seams
- File: .claude/agents/abap-evaluator.md:110
- Evidence: abap-evaluator.md:110-112: 'Fold any newly-accepted priority-2/3 ATC findings into .claude/state/atc-baseline.json… Update .claude/state/abapunit-baseline.json coverage upward' — with the file shape described at line 38 as accepted-WARN-list only (no per_object). The branch adds a second writer: moderniser/src/cli.js:163 writeBaselinePair on verdict --record, keying per_object[sig]. .claude/state/atc-baseline.json _note still reads 'Updated by abap-evaluator on PASS'. docs/MODERNISER_DESIGN.md:327 ratifies split-field ownership and asserts 'concurrent sessions serialize behind the node lock' — grep of the whole moderniser finds no lock of any kind. Probe: ratchetGate({sig S, 3 warns on changed lines}, baseline per_object{S:0}) → BLOCK; same evidence after an evaluator-md-shaped rewrite ({variant, accepted_priority_2_3} only) → {verdict:'PASS', atc_warn_delta:0} — the regression passes as an establish-pass.
- Absence-of-guard proof: Traced every consumer: readBaselines (moderniser/src/cli-io.js:95-100) does JSON.parse with an empty-map fallback and no shape/ownership validation; ratchet.js:71 `per_object?.[key] ?? Infinity` makes a clobbered map indistinguishable from legitimate bootstrap (the seed-∞ tolerance designed for first-pass doubles as the mask). The moderniser side honours carry-through (onPass spreads evaluator fields untouched, tested at ratchet.test.js:186), but the evaluator side has no reciprocal instruction — abap-evaluator.md contains no mention of per_object or field preservation (grep: zero hits), and the branch diff shows .claude/agents/ and .claude/state/ untouched. No cross-process lock, no version/marker field, no schema check guards the pair.
- Proposed fix: Propagate the ratified split-ownership contract to the other writer: amend abap-evaluator.md to (a) preserve all unknown fields (explicitly per_object) verbatim on any baseline write, or better (b) never write the baseline files directly — emit deltas in its verdict JSON and let the owning lane/CLI perform the write (true single-writer). Update both _note fields to name both writers and field ownership. Optionally have writeBaselinePair stamp a marker field (e.g. per_object_owner) so readBaselines can distinguish clobber from bootstrap and fail closed.
- Confidence: CONFIRMED
- Test evidence: moderniser/test/ratchet.test.js:186 covers only the moderniser-side carry-through (passes); no test exercises the evaluator's side of the contract (it is an agent prompt, untestable in-suite) and no test pins the _note/agent contract to the new split ownership — the drift is invisible to the 272-test green suite.
- Adversarial verification: SURVIVED — Every element verified. (1) Second writer confirmed: cli.js:161-164 verdict --record → onPass → writeBaselinePair into .claude/state (default at cli.js:264, pinned by modernise SKILL.md:31), the same files abap-evaluator.md:110-112 instructs the evaluator to rewrite on PASS. (2) No reciprocal contract: grep of abap-evaluator.md for per_object/field-preservation = zero hits; the agent has Write but not Edit (line 116), so its baseline update is a whole-file rewrite; both state files' _note still name the evaluator as sole writer. (3) Fail-open reproduced by live probe against ratchet.js: {sig S, 3 warns on changed lines} vs per_object{S:0} → BLOCK ["warn-delta-regressed:3>0"]; identical evidence vs evaluator-shaped {variant, accepted_priority_2_3} → {verdict:"PASS", atc_warn_delta:0} — indistinguishable from a legitimate establish-pass. readBaselines (cli-io.js:95-100) validates nothing (

### F6 [MEDIUM] CSP hash test compares constant-to-constant, never the emitted script bytes — a wrapping regression would silently kill all report interactivity with the suite green
- Subsystem: analyser-and-deps
- File: analyser/test/html-report.test.js:101
- Evidence: const expected = createHash("sha256").update(APP_JS).digest("base64"); assert.ok(csp.includes("'sha256-" + expected + "'"), "CSP hash matches the emitted APP_JS bytes exactly"); — both the renderer (html-report.js:47 hashes APP_JS) and the test hash the same imported constant; the test never extracts the <script> body from the rendered html string, so the assertion message ('matches the emitted APP_JS bytes exactly') is not what is asserted. If line 40 of html-report.js ever became `<script>\n${APP_JS}</script>` (or any wrapper/second script), the CSP would still contain hash(APP_JS), this test would still pass, and the browser would block the only executable script — tabs, findings browser, and graph all dead, page frozen on the Summary panel.
- Absence-of-guard proof: Traced the whole suite: no test extracts the emitted script content and hashes it, no test asserts emitted-script === APP_JS, and no browser/DOM-level test executes the report, so nothing pins the CSP-hash-to-emitted-bytes seam. My node probe (extract /<script>([\s\S]*?)<\/script>/ from renderHtml output, hash extracted bytes, compare to CSP) shows the CURRENT code is correct (emitted === APP_JS, hashes match) — the gap is purely in the guard, which is the exact guard this branch exists to provide.
- Proposed fix: In the CSP test, extract the executable script from the rendered output and assert on it: const emitted = html.match(/<script>([\s\S]*?)<\/script>/)[1]; assert.equal(emitted, APP_JS, 'emitted script is exactly APP_JS'); then compute expected = sha256(emitted) and assert csp.includes(`'sha256-${expected}'`). Two extra lines, closes the tautology.
- Confidence: CONFIRMED
- Test evidence: Test passes today (398/398). Probe confirms the production code is correct, so this is a Rule-7 'test asserts the wrong thing' finding: the failure scenario is a future regression that this — the only guard on the seam — cannot catch.
- Adversarial verification: SURVIVED — Confirmed by probe. (1) Rule 1: html-report.js:47 and the test's line 101 both hash the same imported APP_JS constant; the test never extracts the emitted <script> body, so the assertion message ("matches the emitted APP_JS bytes exactly") is not what is asserted. (2) Rule 2: grep across analyser/ shows no other test hashes CSP or extracts script bodies; no playwright/jsdom/browser test exists (only doc mentions); renderHtml's only other consumer is cli.js:85 which writes the file unverified — the guard is absent in the whole chain. (3) Rule 8 probe (node, real module): current code correct — emitted executable script === APP_JS and hash matches CSP; regression simulation (<script>\n${APP_JS}</script>) leaves ALL five assertions of the CSP test passing while the hash of the actual script element content mismatches the CSP allowlist, so a browser would block the report's only executable s

### F7 [MEDIUM] Corrupt atc-baseline ceiling (null/string) and corrupt coverage_floor_pct fail OPEN — the equivalent coverage-entry corruption is guarded, the other two numeric baseline reads are not
- Subsystem: moderniser-state-ratchet
- File: moderniser/src/state/ratchet.js:71
- Evidence: ratchet.js:71-72 `const ceiling = baselines.atcBaseline.per_object?.[key] ?? Infinity; atc_warn_delta = Number.isFinite(ceiling) ? delta - ceiling : 0;` and line 83 `const floor = covEntry?.pct ?? baselines.covBaseline.coverage_floor_pct ?? 0;`. Probes: (B1) entry `{s: null}` -> `??` swallows null -> ceiling Infinity -> PASS, atc_warn_delta 0, and onPass RE-establishes the baseline at the observed delta — a corrupt entry silently resets the only-down ratchet ('quality only tightens' violated). (B3) string entry "2" with delta 2 -> Number.isFinite("2") is false -> atc_warn_delta forced to 0 (establish semantics) while the `delta > ceiling` compare coerces — verdict and signed delta desync on corrupt input. (B5) `coverage_floor_pct: "high"` with pct 0.01 -> `0.01 < "high"` is NaN-false -> PASS: a corrupt floor waives the coverage gate entirely.
- Absence-of-guard proof: Lines 80-81 prove the corruption class was considered and deliberately fail-closed — `baseline-corrupt` fires for a covBaseline per_object entry without a finite pct ('must not fall back to the floor') — but the identical validation is absent for the ATC ceiling and the global floor. No caller validates the baseline files: cli-io readBaselines JSON-parses .claude/state/*.json as-is, and those files are git-managed (merge conflicts / hand edits are a real corruption path despite the single-writer contract, which the design doc only asserts for concurrent sessions).
- Proposed fix: In ratchetGate: `const rawCeiling = baselines.atcBaseline.per_object?.[key]; if (rawCeiling !== undefined && !Number.isFinite(rawCeiling)) reasons.push("baseline-corrupt");` (keeping atc_warn_delta at the fail-closed 1), else proceed with `rawCeiling ?? Infinity`. Same check for a present-but-non-finite coverage_floor_pct before line 83.
- Confidence: CONFIRMED
- Test evidence: ratchet.test.js:179-184 pins fail-closed for the covBaseline entry only; no test covers a corrupt ATC ceiling or floor. Probes B1/B3/B5 confirm the fail-open behaviour against the current code.
- Adversarial verification: SURVIVED — CONFIRMED by probes against the real module. B1: per_object entry null → `null ?? Infinity` (ratchet.js:71) → PASS with atc_warn_delta 0, and onPass re-records the baseline at the observed delta — the only-down ratchet silently resets. B5: coverage_floor_pct "high" with pct 0.01 → `0.01 < "high"` NaN-false → PASS, coverage gate waived for any node without a per-object entry. B3 partially holds: a non-numeric string ceiling ("high") is full fail-open like B1; a numeric-string ceiling ("2") desyncs atc_warn_delta (0) from the gate verdict but renderVerdict ANDs gate.verdict with nodeVerdict, so no fail-open escapes for that sub-case. Absence-of-guard verified: cli.js:157 readBaselines → readJson is raw JSON.parse with no schema check; nodeVerdict's isFinite(warn)&&warn<=0 backstop passes awd=0; baselines have no plan.json-style hash verification. Not design-ratified (Rule 5): §6.4 defines 

### F8 [MEDIUM] Seal taxonomy under-approximates: dynamic write-DML, CALL TRANSACTION <var>, CREATE OBJECT TYPE (var), kernel GET/CALL BADI, and PERFORM form(prog) ON COMMIT are all silently unscanned (L5 forbids under-approximation)
- Subsystem: moderniser-graph
- File: moderniser/src/graph/augment.js:29
- Evidence: Probe (node ESM import of overApproximateEdges): 'UPDATE (lv_tab) SET amount = 0.' -> unsealed; 'INSERT (lv_tab) FROM @ls_row.' -> unsealed; 'MODIFY (lv_tab) FROM TABLE @lt.' -> unsealed; 'DELETE (lv_tab) FROM TABLE @lt.' -> unsealed (only the FROM-( form 'DELETE FROM (lv_tab)' seals via SEAL_PATTERNS[2]); 'CALL TRANSACTION lv_tcode.' -> unsealed; 'LEAVE TO TRANSACTION lv_tcode.' -> unsealed; 'CREATE OBJECT lo TYPE (lv_class).' -> unsealed; 'GET BADI lr_badi... CALL BADI lr_badi->process.' -> unsealed; 'PERFORM upd_db(zprog) ON COMMIT.' -> no seal AND no synthetic edge (the RESOLVABLE capture [\w~/]+ stops at '(' so \s+ON never matches, and SEAL_PATTERNS[3] requires the paren directly after PERFORM).
- Absence-of-guard proof: scanNode (augment.js:73-91) is the ONLY consumer of node.source; its three pattern sets (SEAL_PATTERNS:29-40, RAW_SEAL_PATTERNS:45, RESOLVABLE:49-52) were each checked against the probed constructs — none matches. The design doc's Stage-1 scan list (docs/MODERNISER_DESIGN.md:106) omits all of these, and its 'Known line-scan limits' register records only two other gaps (continuation-line CALL FUNCTION, bare-variable SUBMIT) — so these are undocumented under-approximations, the direction §3.1/L5 explicitly forbids ('over-approximate, never under'). A missed seal lets a node with an unknowable runtime callee/table set dispatch without the NEEDS_MANUAL_SEAM human confirmation (frontier.js:54 only filters what augment seals).
- Proposed fix: Add SEAL_PATTERNS entries: /\b(?:UPDATE|INSERT|MODIFY|DELETE)\s+\(\s*[\w~/]+\s*\)/i (dynamic DML target), /\b(?:CALL|LEAVE\s+TO)\s+TRANSACTION\s+(?!')\S/i, /\bCREATE\s+OBJECT\s+\S+\s+TYPE\s*\(/i, /\b(?:GET|CALL)\s+BADI\b/i; and either extend the PERFORM-ON-COMMIT regex to capture form(prog) or seal on it. Alternatively, if any of these is consciously out of scope, record it in the design doc's known-limits register — silence is the defect either way (doc gap = code gap).
- Confidence: CONFIRMED
- Test evidence: 272/272 tests pass but none exercises these constructs; the test named 'runtime-codegen and dynamic-dispatch families all seal (taxonomy completeness)' (augment.test.js:223) asserts completeness in its name while covering only the six implemented families — a too-narrow assertion per Rule 7.
- Adversarial verification: SURVIVED — CONFIRMED, not refuted. Rule 8 probe re-run against moderniser/src/graph/augment.js reproduces every evidence claim: all nine constructs (dynamic write-DML UPDATE/INSERT/MODIFY/DELETE (lv_tab), CALL TRANSACTION <var>, LEAVE TO TRANSACTION <var>, CREATE OBJECT ... TYPE (var), GET/CALL BADI, PERFORM form(prog) ON COMMIT) yield sealed:false with no synthetic edge, while controls DELETE FROM (lv_tab) and SELECT FROM (lv_tab) seal — confirming reads seal but writes do not. Rule 2 trace: scanNode is the only node.source consumer; seals reach the plan only via assemblePlan opts.augment.seals (assemble.js:39), sole producer overApproximateEdges, and cli.js:50 passes no augment; frontier.js:54 filters only augment-produced seals; the analyser's get-badi edge targets the handle VARIABLE (statement-edges.js:87-89), not the implementations, so no compensating edge exists. Rule 5: the design doc's 'K

### F9 [MEDIUM] Synthetic-edge target tokens (bare form names, 'ZCL_X=>METH') match neither the CPG node-id space ('OBJ.METH') nor the object-graph id space, and dangling edges are silently dropped by condense — the 'synthetic back-edge must reach Tarjan' guarantee no-ops with no error
- Subsystem: moderniser-graph
- File: moderniser/src/graph/augment.js:85
- Evidence: augment.js:85 emits `target: t.toUpperCase()` — probe shows 'SET HANDLER zcl_evt=>on_done FOR lo.' emits target 'ZCL_EVT=>ON_DONE' while the analyser fixture's method node ids use dots ('CL_GUI_FRONTEND_SERVICES.GUI_UPLOAD') and the object graph uses bare object ids ('ZCL_EVT'). Probe through assemblePlan: an augment edge {source:'ZB', target:'ZA'} (valid ids) correctly fuses ZA+ZB into one break_gate super-node; the same dependency expressed with the token augment actually emits ({source:'ZB', target:'FRM_LATE_IN_ZA'}) produces two independent singleton nodes, no break_gate, no error, no warning — the cycle Tarjan exists to catch silently disappears.
- Absence-of-guard proof: tarjanCondense (condense.js:21) silently skips any edge whose endpoint is not a known node (`if (adj.has(u) && adj.has(v))`); assemble.js:38 concatenates opts.augment.edges into the graph with no endpoint validation — in the SAME function that fail-closes on a plan object missing from the graph (assemble.js:49), so the fail-closed precedent exists but does not cover this seam. Nothing in augment.js, assemble.js, or any adapter maps '=>'-qualified or bare-form tokens to owning objects; no module warns on a dropped edge.
- Proposed fix: Two-part: (1) in augment.js, normalise emitted targets to the owning object id (split at '=>' for class-method handlers; document that perform-on-commit targets are program-local and thus intra-object) and state the target id space in the JSDoc contract; (2) in assemblePlan, fail closed on an augment edge whose endpoint is not in the object graph — either throw, or conservatively set the SOURCE node's dynamic_seal (unresolvable target = exactly what NEEDS_MANUAL_SEAM means). Add a test pinning the dangling-target behaviour.
- Confidence: CONFIRMED
- Test evidence: assemble.test.js:146 ('opts.augment threads Stage-1 seals and synthetic edges') passes but only uses targets that are valid object ids; no test in augment.test.js or assemble.test.js exercises a synthetic edge whose target is the token format augment actually emits for cross-object handlers.
- Adversarial verification: SURVIVED — Confirmed by direct probe through the real modules: overApproximateEdges on 'PERFORM frm_late_in_za ON COMMIT.\nSET HANDLER zcl_evt=>on_done FOR lo.' emits targets 'FRM_LATE_IN_ZA' and 'ZCL_EVT=>ON_DONE' (augment.js:85), which match neither the CPG id space (fixture forms/methods are dot-qualified, e.g. ZFICO_BTC_CSV_GL.UPD_PROTOCOLL; zero ids contain '=>') nor the object-graph id space. Feeding that actual output into assemblePlan via opts.augment produced two independent singleton nodes, break_gate:false, no error/warning; the same dependency with valid object ids produced one break_gate super-node. Guard absence traced end-to-end: condense.js:20-21 silently skips unknown endpoints; assemble.js:38 concatenates augment edges with no endpoint validation (while line 49 fail-closes the adjacent plan-object seam); the literal-target path never sets dynamic_seal so the seal veto does not com

### F10 [MEDIUM] Stale green verdict survives PARK / NEEDS_MANUAL_SEAM re-entry and blesses a REGENERATED artifact
- Subsystem: moderniser-sched
- File: moderniser/src/sched/loop.js:227
- Evidence: setStatus clears verdict_green only on the retry edges: `const retry = to === "GENERATED" && (from === "SYNTAX_OK" || from === "GATED")`. Probe: record verdict {green:true} at GATED → applyProgress GATED→NEEDS_MANUAL_SEAM → PENDING (human confirms) → dispatch → GROUNDED→GENERATED→…→GATED → `applyOutcome(..., {status:'GREEN'})` with NO new verdict → output: "stale verdict_green after re-entry + regeneration: true / GREEN ACCEPTED on the STALE verdict — status: GREEN". Same path exists via BLOCK(NO_RELEASED_SUCCESSOR)→PARK→PENDING.
- Absence-of-guard proof: The re-entry transition GROUNDED→GENERATED is not a 'retry' edge, so the line-227 clear (`delete verdict_green[sig] // a stale verdict can never bless a REGENERATED artifact`) never fires; applyProgress's PARK→PENDING branch (lines 109-118) clears park_register and deferral_track but not verdict_green. The cycle counter also does not increment on this path, so cli.js parityAttestation's `decided_cycles[sig] === state.cycle[sig]` temporal binding would equally accept a pre-re-entry attestation — the same root cause defeats two bindings.
- Proposed fix: Clear verdict_green on ANY entry into GENERATED (change the clear condition from `retry` to `to === "GENERATED"`), or additionally clear it in the PARK/NEEDS_MANUAL_SEAM→PENDING re-entry branch next to the register cleanup. Consider also bumping cycle (or a generation counter) on re-entry so attestation bindings distinguish artifacts.
- Confidence: CONFIRMED
- Test evidence: loop.test.js:224-232 ('a re-generated node's stale verdict is cleared on the retry edge') exercises only GATED→GENERATED; no test covers verdict state across PARK or NEEDS_MANUAL_SEAM re-entry. Suite green.
- Adversarial verification: SURVIVED — Executable probe against the real modules confirms both paths: verdict {green:true} recorded at GATED survives GATED→NEEDS_MANUAL_SEAM→PENDING and BLOCK(NO_RELEASED_SUCCESSOR)→PARK→PENDING re-entry, the regeneration edge GROUNDED→GENERATED never triggers the loop.js:220-228 retry-edge clear, and applyOutcome(GREEN) is ACCEPTED on the stale verdict after full regeneration with no new recordVerdict (probe output: "GREEN ACCEPTED on the stale verdict — status: GREEN" on both paths). Grep proves verdict_green has exactly one setter (recordVerdict, loop.js:66) and one clear site (the retry branch); applyProgress's PARK→PENDING branch clears only park_register/deferral_track; cmdOutcome (cli.js:93-107) adds no shell-level guard. The cycle-counter side-claim also verified: cycle stays 0 through re-entry, so cli.js:119's decided_cycles temporal binding would accept a pre-re-entry attestation. No

### F11 [MEDIUM] dispatch() lacks the dynamic_seal veto — a sealed node can be handed to node drivers, bypassing the L5 human-seam gate
- Subsystem: moderniser-sched
- File: moderniser/src/sched/loop.js:96
- Evidence: dispatch re-checks readiness (`indegree !== 0 → throw`) and park (`park_register.some(...) → throw`) defensively but never reads the plan node's dynamic_seal. Probe: assemblePlan with `augment: {seals: {ZS: true}}` → nextDispatch correctly returns only ZFREE, but `dispatch(plan, st, [sealedSig])` → "DISPATCH OF SEALED NODE ACCEPTED — status now: GROUNDED".
- Absence-of-guard proof: The seal is enforced solely in nextFrontier (frontier.js:54 `s.dynamic_seal !== NEEDS_MANUAL_SEAM`); initRun sets sealed nodes to plain PENDING so the FSM check in setStatus (PENDING→GROUNDED legal) cannot catch it. cli.js cmdDispatch (line 71-79) accepts arbitrary prose-supplied sigs, so a sealed sig can be grounded in production without the human caller-set confirmation the design requires ('blocks signature-changing modernisation until a human confirms the caller set', MODERNISER_DESIGN §3.1 Stage 1). dispatch's asymmetry (park re-checked, seal not) contradicts its own defensive-guard rationale.
- Proposed fix: In dispatch, after the park check add: `const n = plan.nodes.find((x) => x.id === sig); if (n.dynamic_seal === "NEEDS_MANUAL_SEAM") throw new Error(`loop: ${sig} is dynamic-sealed — a human must confirm the caller set (L5)`);`.
- Confidence: CONFIRMED
- Test evidence: assemble.test.js:161-171 ('a sealed plan node is never dispatched') asserts only the nextDispatch/frontier veto — the test name overclaims; no test calls dispatch() with a sealed sig. Suite green.
- Adversarial verification: SURVIVED — CONFIRMED at MEDIUM. Probe (node, live imports from moderniser/src) reproduces exactly: assemblePlan with augment.seals {ZS:true} → initRun sets the sealed node to plain PENDING (loop.js:39) → nextDispatch returns only ZFREE (frontier veto works) → dispatch(plan, st, [sealedSig]) is ACCEPTED, status GROUNDED, and applyProgress walks it on to GENERATED — no later guard. Absence-of-guard traced end-to-end: the seal is read in exactly one executable place (frontier.js:54); dispatch (loop.js:88-102) defensively re-checks two of the three frontier vetoes (indegree, park) but not dynamic_seal; nodeVerdict (node/verdict.js) has no seal/caller-set conjunct; the closed 7-kind escalation taxonomy (escalation-bus.js:21-29, gate-ui.js DECISIONS) has NO seam-confirm kind, so no audited human decision can gate it; cmdDispatch (cli.js:71-80) forwards arbitrary prose-supplied sigs. Rule 5 design-intent 

### F12 [MEDIUM] augment→assemble seam shape mismatch: overApproximateEdges output (node-level seals/edges) silently no-ops through opts.augment — under-approximation, the direction L5 forbids
- Subsystem: moderniser-sched
- File: moderniser/src/sched/assemble.js:38
- Evidence: assemble consumes `opts.augment.edges` by appending to the OBJECT-level og.edges and `opts.augment.seals` keyed by object-level members, but augment.js emits node-level output (seals keyed by CPG node id, synthetic targets as uppercased form/handler names). Probe: augment emits `seals = {"ZA.FORM1":true}` and synthetic `ZA.FORM1 → ZB_FORM`; feeding that into assemblePlan yields "ZA | dynamic_seal: NONE | break_gate: false" — the seal vanished and the cycle-closing edge was dropped (tarjanCondense filters unknown endpoints at condense.js:21 and :100 with no error).
- Absence-of-guard proof: No adapter exists anywhere in moderniser/src to collapse node-level augment output to object level (grep: only assemble.js:38-39 and tests reference the seam), and assemblePlan performs no fail-closed validation of augment inputs: an unmatched seal key or unknown edge endpoint is silently discarded rather than thrown (contrast the explicit throw for a plan object missing from the graph, assemble.js:49). The production CLI (cmdPlan, cli.js:47-64) never wires augment at all, so the Stage-1 seam is currently exercised only by tests that hand-craft the object-level shape. Even at object level, synthetic targets like 'LCL_H=>ON_CHANGED' can never match an object id without a resolution step that does not exist.
- Proposed fix: Make assemblePlan fail closed on augment inputs: throw if any seals key is not a known object/graph node and if any augment edge endpoint is unknown to the object graph; ship the missing collapse adapter (node-id → owning object via doc.graph.nodes, resolving synthetic targets against CPG node/object ids, sealing when unresolvable) before the source-extraction step lands.
- Confidence: CONFIRMED
- Test evidence: assemble.test.js:146-159 passes hand-crafted object-keyed seals/object-level edges; augment.test.js never composes overApproximateEdges output into assemblePlan. Suite green — nothing exercises the real composition.
- Adversarial verification: SURVIVED — Probe (node, importing repo modules) reproduces the finding exactly: overApproximateEdges on a realistic CPG (node ids OBJECT.MEMBER) emits seals={"ZA.FORM1":true} and synthetic edge ZA.FORM1→ZB_FORM; feeding that into assemblePlan opts.augment yields 'ZA | seal:NONE | break_gate:false' — the seal never matches object-level members (assemble.js:60) and tarjanCondense silently drops the unknown-endpoint edge (condense.js:21, :100). The hand-adapted object-level shape works, but augment can never emit it (targets are form/handler names, seals keyed by node id). The charitable alternative wiring — augmenting the CPG before buildObjectGraph per §3.1's literal order — ALSO loses the seal (buildObjectGraph/scopeNodes never propagate dynamic_seal) and creates a spurious 'ZB_FORM' object instead of closing the cycle. Guard absence confirmed: grep shows no adapter/resolver in moderniser/src; free

### F13 [MEDIUM] Removed CDS DCL restriction is invisible to auth-coverage LOSS — auth can weaken to GREEN through the fail-open conjunct
- Subsystem: moderniser-node-loop
- File: moderniser/src/node/invariants.js:34
- Evidence: const lostScopes = b.auth_checks
    .filter((c) => !afterPairs.has(pairKey(c)) && !afterObjects.has(c.object))
    .map(...);  // loss is derived ONLY from before.auth_checks; b.dcl_restrictions is read solely for auth_delta (line 43)
Probe: invariantDiff({ dcl_restrictions: [{ object: 'S_TABU_DIS' }] }, {}) → { lost: false, scopes: [], delta: true } — a deleted DCL (row-level auth on a CDS/RAP node) is NOT a loss. Same with an unrelated after-check (P2 probe: lost:false).
- Absence-of-guard proof: The module's own docstring (lines 12-15) defines LOSS as 'a before-covered (object,field) not covered in the after by an AUTHORITY-CHECK OR a DCL grant', and defines effective scope as pairs ∪ DCL restrictions — a before-DCL-covered (object,*) scope is before-covered, yet no code path ever tests before.dcl_restrictions against after coverage. Downstream, verdict.js line 70 blocks only on auth_coverage.lost === true (ratified fail-OPEN), so lost:false → GREEN-able; the only firing signal, auth_delta:true, has zero consumers (grep over moderniser/src: only invariants.js produces it; cli.js:154 nulls attestations.auth_equivalence 'until its consumer is wired'). parity.js's auth_vanished veto ('auth removed, no relocated DCL grant') could catch it, but that flag's extractor is equally unbuilt — no guard exists in any built call chain.
- Proposed fix: Add the symmetric check: for each before DCL object not covered in the after (no after DCL on the same object — and, if AUTHORITY-CHECK is accepted as an equivalent relocation, no after auth_check pair on that object), append { object, field: '*' } to lost_scopes. Two-line filter over b.dcl_restrictions mirroring the existing b.auth_checks filter, plus a test for the DCL-removed case.
- Confidence: CONFIRMED
- Test evidence: no-test — invariant-diff.test.js exercises DCL only on the AFTER side (relocation tests, lines 48-74); no test covers a before-DCL removed in the after; suite passes 272/272 so no assertion contradicts the probe.
- Adversarial verification: SURVIVED — Probe confirms every mechanical claim: invariantDiff({dcl_restrictions:[{object:'S_TABU_DIS'}]},{}) → lost:false/auth_delta:true (control: removed AUTHORITY-CHECK → lost:true); verdict.js:70 blocks only on lost===true; auth_delta has zero built consumers; cli.js:154 nulls attestations.auth_equivalence 'until its consumer is wired (build-step 7)'; parity's auth_vanished extractor is an unbuilt later I/O step; no test covers before-DCL removal. So a dropped DCL can GREEN (and persist ratchet baselines) today. BUT the HIGH defect classification fails Rule 5: the locked design (docs/MODERNISER_DESIGN.md §3.2 'Auth-COVERAGE invariant [L7]', line 219) defines LOSS exhaustively as a removed CHECK not mapped to a DCL grant, or WITH PRIVILEGED ACCESS on a row-auth CDS — removed DCL is deliberately delta-not-loss, guarded by 'on any non-empty auth delta, an auth-equivalence attestation before PASS

### F14 [MEDIUM] Pre-existing WITH PRIVILEGED ACCESS is re-counted as a fresh coverage loss on every diff — permanent false BLOCK, inconsistent with auth_delta
- Subsystem: moderniser-node-loop
- File: moderniser/src/node/invariants.js:37
- Evidence: const privilegedLoss = a.privileged_cds
    .filter((c) => c.had_row_auth === true)
    .map((c) => ({ object: c.object, field: '*' }));  // reads ONLY the after bundle; before.privileged_cds is never subtracted
Probe: invariantDiff(b, b) with b = { privileged_cds: [{ object: 'I_X', had_row_auth: true }] } → { lost: true, scopes: [{object:'I_X',field:'*'}], delta: false } — a 'loss' with a provably unchanged auth footprint.
- Absence-of-guard proof: invariantDiff is a before/after DIFF (P4 'auth NOT weakened' by THIS change; L6.2 pre-existing debt is carried, not counted), but no code compares after.privileged_cds against before.privileged_cds for the loss computation — before's set feeds only the auth_delta setEqual (lines 44-46), which correctly reports no change while lost=true blocks. verdict.js line 70 then BLOCKs the node on every refinement cycle for a condition its rewrite did not introduce and (if the privileged access is legitimately pre-existing) cannot fix — an unfalsifiable BLOCK the bounded retry loop converts into a hard quarantine.
- Proposed fix: Count only INTRODUCED privileged access: filter a.privileged_cds entries with had_row_auth===true whose object is not already privileged in b.privileged_cds (e.g. const beforePrivObjs = new Set(b.privileged_cds.map(c=>c.object)); … .filter(c => c.had_row_auth === true && !beforePrivObjs.has(c.object))). Add the unchanged-both-sides regression test asserting lost:false.
- Confidence: CONFIRMED
- Test evidence: no-test — invariant-diff.test.js line 56 covers only the introduction case (before bundle empty); no test has privileged_cds populated on both sides; suite green.
- Adversarial verification: SURVIVED — CONFIRMED at MEDIUM. Probe reproduced exactly: invariantDiff(b,b) with b={privileged_cds:[{object:'I_X',had_row_auth:true}]} returns lost=true, lost_scopes=[{object:'I_X',field:'*'}], auth_delta=false — a coverage 'loss' on an identical before/after bundle. Mechanism: invariants.js:37-39 filters only a.privileged_cds; b.privileged_cds feeds only the auth_delta setEqual (lines 44-48), so pre-existing WITH PRIVILEGED ACCESS is re-counted as fresh loss on every cycle while the delta correctly reports no change. verdict.js:56/:70 makes lost=true an unconditional, non-attestable, non-PARKable BLOCK. Design-intent defence fails: the same lost_scopes list computes its auth_checks term before-relatively (line 34-36), P4a is documented as 'auth NOT weakened' (a diff property), L6.2 says pre-existing debt is carried not counted, and remediation commit da17102 already classified the identical 'read

### F15 [MEDIUM] auth_delta has no consumer and the GREEN conjunction has no auth-attestation conjunct — L7's 'attestation owed before PASS' is fail-open while deferred
- Subsystem: moderniser-node-loop
- File: moderniser/src/node/verdict.js:70
- Evidence: if (cp.auth_coverage?.lost === true) reasons.push("auth-coverage-lost"); // the ONLY auth conjunct — nothing consumes invariantDiff.auth_delta or attestations.auth_equivalence
Grep proof: 'auth_delta' appears only in invariants.js (producer) and its test; cli.js:154 hard-nulls it: attestations: { …, auth_equivalence: null } with the comment 'nulled until its consumer is wired (build-step 7)'.
- Absence-of-guard proof: MODERNISER_DESIGN §3.2 L7 (line 219): 'On any non-empty auth delta, an abap-security-reviewer must sign an auth-equivalence attestation (persisted in attestations) before PASS.' No conjunct in nodeVerdict, no gate in ratchetGate, and no escalation-raising code path enforces it — the AUTH_EQUIVALENCE escalation kind and its ATTEST/REJECT verbs exist (gate-ui.js:18) but nothing raises or joins them. The deferral is documented (cli.js:150-151, design build-order step 'security-reviewer attestation wiring'), so this is ratified sequencing — but the interim direction is fail-OPEN (auth footprint changes render GREEN with no owed sign-off), and with finding 1 auth_delta is the ONLY signal that fires on a dropped DCL. Mitigation: GREEN online requires DEV credentials (BLOCKED-ON-DEV-CREDS) and a human releases transports — caps severity at MEDIUM.
- Proposed fix: Until build-step 7 wires the attestation join, fail closed instead of open: add a verdict conjunct `if (cp.invariants?.auth_delta === true && !nonEmptyString(cp.attestations?.auth_equivalence)) reasons.push("auth-delta-unattested")` (mirroring passesParity), and have the checkpoint assembler carry auth_delta from invariantDiff. Alternatively record the interim fail-open direction as an explicit ratified decision in §6.1/§3.2.
- Confidence: CONFIRMED
- Test evidence: no-test asserts auth_delta gates anything; cli.test.js verifies only that a forged auth_equivalence field is nulled (the deferral), not any enforcement; invariant-diff.test.js line 53 asserts 'attestation still owed' in prose only.
- Adversarial verification: SURVIVED — CONFIRMED at MEDIUM. Live probe: invariantDiff({dcl_restrictions:[{object:'ZOBJ'}],commit_work:1}, {commit_work:1}) → {intact:true, auth_coverage:{lost:false}, auth_delta:true}; feeding that checkpoint (all other conjuncts green, attestations.auth_equivalence:null as cli.js:154 forces) into nodeVerdict → {verdict:'GREEN', reasons:[]}. Repo-wide grep: auth_delta has zero code consumers (producer invariants.js:47-54 + its test + SKILL.md prose only); verdict.js:70 is the sole auth conjunct and is documented fail-open; ratchetGate/renderVerdict take no auth input; nothing raises AUTH_EQUIVALENCE and cli.js joins only PARITY_REVIEW rows (114-121), so even a human REJECT has no machine effect. Design L7 (line 219) demands attestation 'before PASS' while the formal GREEN conjunction (224-228) and build order (252, 524-525) ratify the deferral — interim direction fail-open as stated. Rule 7: no

### F16 [MEDIUM] sweep-order's dependency `swept` flag conflates drafted and failed — the SKILL's "grounding against its dependencies' drafts (they exist)" guarantee breaks on a failed dep
- Subsystem: cli-skill-exception
- File: moderniser/src/cli.js:192
- Evidence: `swept: ledger.swept[d] !== undefined` — probe E: after `sweep-mark <dep> --result failed`, the dependent's sweep-order view reports `{"sig":…,"status":"SYNTAX_OK","swept":true}` with no result field. SKILL.md step 5b tells the generator to ground "against its dependencies' drafts (they exist — deps sort earlier)" — false when the dep's sweep result was `failed` (no draft was produced). Secondary: sweep-mark accepts non-PENDING nodes (a gated-pass SYNTAX_OK node was markable), polluting the ledger for proof-bundle assembly.
- Absence-of-guard proof: The result string is stored in the ledger (`ledger.swept[sig] = { result, ts }`) but cmdSweepOrder discards it; nothing else in the payload or the SKILL procedure distinguishes a drafted dep from a failed one.
- Proposed fix: Emit `sweep_result: ledger.swept[d]?.result ?? null` in the dependencies array (and optionally reject sweep-mark on nodes not PENDING in loop state); amend SKILL step 5b to ground failed-sweep deps against original source.
- Confidence: CONFIRMED
- Test evidence: passing tests exist for sweep-order/sweep-mark (cli.test.js:161,199) but neither marks a dependency `failed` before reading the dependent's view — the conflation is unexercised.
- Adversarial verification: SURVIVED — Confirmed by probe against scratch --state-dir/--runs-dir: after `sweep-mark <dep> --result failed`, the dependent's sweep-order view reports the dep as {"status":"PENDING","swept":true} (and {"status":"SYNTAX_OK","swept":true} in probe E's variant) — bit-identical to a drafted dep, because cli.js:192 emits `swept: ledger.swept[d] !== undefined` and discards the stored result (cli.js:209). Full-tree grep shows no consumer of `.result` except sweep-mark's own idempotence check; SKILL.md 5b affirmatively guarantees deps' drafts "exist — deps sort earlier" and prescribes sweeping IN ORDER with no failed-dep handling; MODERNISER_DESIGN §6.5 never ratifies failed-dep semantics despite the CLI contract anticipating `--result failed`. Secondary claim also confirmed: cmdSweepMark (cli.js:202) checks only plan membership — a gated-pass SYNTAX_OK node was markable in the probe. Tests cli.test.js:1

### F17 [MEDIUM] Re-park after re-entry silently drops the second park's audit row — park-register.json keeps the first sign-off, justification, and successor probe
- Subsystem: cli-skill-exception
- File: moderniser/src/cli.js:126
- Evidence: parkAudit: `if (reg.parked.some((p) => p.node_id === sig)) return;`. Probe: park signed by alice (probe I_FIRST) → `progress PENDING` re-entry → re-dispatch → BLOCK → second PARK signed by bob (probe I_SECOND, different justification) → accepted; park-register.json still reads `[{by: alice, j: "first park: registry gap", probe: I_FIRST}]` while loop state carries bob's row. The audited L7 register misrepresents the live park decision, and a future successor re-probe would check I_FIRST, not I_SECOND.
- Absence-of-guard proof: The idempotency check keys on node_id presence alone (built for crash-retry, where flags are identical); tryPark's own "already parked" throw is unreachable through the CLI because parkAudit returns first; park.js reEnter — which would remove the row on re-entry — has no production caller, so the stale row persists across the re-park.
- Proposed fix: Make the idempotency check content-aware (skip only when node_id AND signed_by AND justification AND probe match — the crash-retry case); on a genuine re-park, append a new row (the register is an audit log, not a set). Alternatively remove the row on PARK→PENDING re-entry through a CLI verb wrapping reEnter.
- Confidence: CONFIRMED
- Test evidence: cli.test.js:245 covers first-park enforcement and canonicalisation only; no test re-parks after re-entry.
- Adversarial verification: SURVIVED — Confirmed by end-to-end probe against scratch dirs: park signed by alice (probe I_FIRST) → `progress PENDING` re-entry (FSM-legal, node-status.js:53) → re-dispatch → BLOCK → second PARK signed by bob (probe I_SECOND, different justification) exits 0, yet park-register.json still holds only alice's row (I_FIRST, first justification) while live state.park_register carries bob's row. Guard trace: cli.js:126 keys on node_id presence alone and returns before tryPark, making park.js:24's "already parked" throw unreachable; reEnter (park.js:49), the only code that removes the audited row on re-entry, has no production caller (grep: park.test.js only); loop.js:109-117 cleans live state only. log.jsonl does capture both signed_by names but neither justification nor successor_probe, so bob's justification and I_SECOND are recorded nowhere. Not ratified design: MODERNISER_DESIGN.md:268 defines regi

### F18 [MEDIUM] SKILL.md promises behaviours with no executable path: the park successor re-probe ("re-checks each run") and GatePacket presentation — reEnter, renderPacket, levelDisposition, clusterOscillations are wired to nothing
- Subsystem: cli-skill-exception
- File: .claude/skills/modernise/SKILL.md:69
- Evidence: SKILL escalation table: "the probe re-checks each run" (line 69) and "Present each surfaced escalation as its GatePacket (kind, one-line cause, deterministic evidence, plan fields, typed decisions) — never invent decision options" (line 78). Grep across moderniser/src: park.js reEnter, gate-ui.js renderPacket, risk-gate.js levelDisposition, oscillation.js isOscillating/clusterOscillations have zero importers outside test files; cmdEscalations returns raw register rows with no cause or decision set; no CLI verb performs the cloudification-registry probe on resume or any tick.
- Absence-of-guard proof: A fresh session executing the SKILL as written has no command for either behaviour: parked nodes never auto-re-enter (and their audit rows are never released), and the typed decision set per kind is obtainable only by reading gate-ui.js source. Partially mitigated: `decide` fail-closes on any decision outside the typed set, so an invented option is refused rather than recorded. Unlike auth_equivalence (explicitly deferred to build-step 7 in a cli.js comment), no ratified deferral covers these — the branch's own commit message claims "executable wiring" for the exception family.
- Proposed fix: Add two thin verbs: `packet <run_id> <esc_id>` (renderPacket over the register row + evidence/plan-field context) and a resume-time or explicit `reprobe <run_id>` step wrapping reEnter against the greenfield registry; or amend SKILL.md to mark both as deferred so a fresh session does not chase a nonexistent command.
- Confidence: CONFIRMED
- Test evidence: reEnter/renderPacket/levelDisposition/clusterOscillations each have passing unit tests (park.test.js, gate-ui.test.js, risk-gate.test.js, oscillation.test.js) — the modules work; the gap is wiring, which no test can cover because no production call site exists.
- Adversarial verification: SURVIVED — Live CLI probe (scratch state-dir): parked a node with --successor-probe I_JOURNALENTRYTP, then ran resume/next — no registry probe fired, node stayed parked, and after a manual progress PARK->PENDING the audited park-register.json row was still present (reEnter is the only releasing function; whole-repo grep shows zero production importers for reEnter, renderPacket, levelDisposition, clusterOscillations — test files only). `escalations` returned raw rows {id, kind, node_ids, status, opened_at} with no cause/decisions/evidence, so the SKILL:78 GatePacket contract has no executable path. No ratified deferral covers these: MODERNISER_DESIGN §4 step 6 includes "park + successor re-probe" and "gate_ui packet/decision contract" in the exception family that commits 1308df1/9352bcf declare complete with "executable wiring" (only auth_equivalence is deferred to step 7 per cli.js:150). Mitigation

### F19 [MEDIUM] L1 super-node artifact skeleton omits .dcls/.ddlx/service-binding/IAM — the exact surface L1's HIGH-severity rationale mandates; design is internally split (L1 8-artifact list vs §3.3#6 5-type _TRANSPORT_ORDER) and code implements the narrower list
- Subsystem: docs-contracts
- File: moderniser/src/sched/assemble.js:135
- Evidence: const RAP_SURFACE = [["cds", 1], ["intf", 2], ["class", 3], ["bdef", 4], ["test_class", 5]]; — vs MODERNISER_DESIGN.md:70 (L1): "ONE super-node over the full Clean-Core surface (BDEF + impl + CDS iface + projection + service binding + .dcls DCL + .ddlx + IAM)" with justification "bundling RAP but not .dcls ships an open data surface while P4 reads green. (HIGH)". MODERNISER_DESIGN.md:338 (§3.3#6) pins _TRANSPORT_ORDER to exactly the 5 types the code has.
- Absence-of-guard proof: The plan artifact is deep-frozen (plan.js deepFreeze), so the node driver cannot add artifact slots later; grep for dcls/ddlx/srvb/iam across moderniser/src returns nothing outside comments. The L1 rule "green only when ALL artifacts activated AND reconciled" will iterate the skeleton, which has no DCL/binding slot — no code path can reconcile an artifact type absent from the frozen skeleton.
- Proposed fix: Pick one authoritative surface list: either extend RAP_SURFACE (+ dcls/ddlx/srvd/srvb/iam ranks) in assemble.js and §3.3#6's _TRANSPORT_ORDER, or amend L1/§3.2 to state explicitly that the frozen skeleton is the 5-type offline core and the DEV-gated checkpoint build must extend the artifact set before reconciliation — recording which doc clause supersedes which.
- Confidence: CONFIRMED
- Test evidence: moderniser/test/assemble.test.js passes while pinning the 5-type skeleton; no test asserts a dcls/ddlx/binding artifact slot — the tests pin the narrower of the two contradictory doc clauses.
- Adversarial verification: SURVIVED — CONFIRMED by probe + trace. Probe (real code path): assemblePlan on a 'RAP Business Object' target yields artifacts ["cds","intf","class","bdef","test_class"] only; pushing a dcls slot throws TypeError (deep-frozen via plan.js deepFreeze), and loadPlan rejects any hash-mismatched plan — so no downstream driver can widen the skeleton. The locked design mandates the wider surface in FOUR places (MODERNISER_DESIGN.md:70 L1 8-type incl .dcls/.ddlx/binding/IAM; :113 §3.1; :169 §3.2; :338 §3.3#6's expansion clause itself names 7 types — only its _TRANSPORT_ORDER tiebreak map is 5-type), and TALOS_MODERNISER_INTERNALS.md:500-501 explicitly says extend to srvd/srvb/ddlx/dcls. Rule 5 check: introducing commit 8c2d73c fixed 'L1 expansion silently dropped (a LOCKED rule)' but implemented only the 5 _TRANSPORT_ORDER keys while calling it 'the typed Clean-Core-surface skeleton' — partial remediation,

### F20 [MEDIUM] L7 auth-equivalence attestation is doc-promised as gating PASS but enforced by nothing: auth_delta has zero consumers, cmdVerdict hard-nulls attestations.auth_equivalence, and SKILL.md presents the gate as live without a pending marker
- Subsystem: docs-contracts
- File: .claude/skills/modernise/SKILL.md:68
- Evidence: SKILL.md:68: "a named human signs the attestation before PASS"; SKILL.md:44: "abap-security-reviewer signs the auth-equivalence attestation whenever the auth footprint changed (auth_delta)"; MODERNISER_DESIGN.md:219 (§3.2 L7): "On any non-empty auth delta, an abap-security-reviewer must sign an auth-equivalence attestation (persisted in attestations) before PASS." — but cli.js:154 sets attestations: { ..., auth_equivalence: null } and node/verdict.js has no auth_delta input (auth conjunct blocks only on auth_coverage.lost === true).
- Absence-of-guard proof: Grep across moderniser/src: auth_delta is computed in node/invariants.js:47 and consumed nowhere; nodeVerdict renders GREEN with auth_delta=true and no attestation. An AUTH_EQUIVALENCE escalation can be decided (ATTEST) into escalations.json, but parityAttestation joins only kind===PARITY_REVIEW — no join path exists for auth. Deferral is recorded only in a cli.js comment ("until its consumer is wired, build-step 7") and design build-order step 7, not in the operative SKILL text.
- Proposed fix: Either wire the conjunct now (nodeVerdict blocks when checkpoint.auth_delta===true without a named auth_equivalence attester; CLI joins it from AUTH_EQUIVALENCE register rows with the same run+cycle temporal binding as parity), or mark the attestation rows in SKILL.md and design §3.2 as PENDING/BLOCKED-ON-DEV so the runbook does not present an unenforced security gate as operative.
- Confidence: CONFIRMED
- Test evidence: No test asserts auth_delta implies attestation-before-GREEN; verdict.test.js attestation tests cover parity_equivalence only; invariant-diff.test.js covers auth_delta computation but not consumption — consistent with an unwired consumer.
- Adversarial verification: SURVIVED — Confirmed on every claim. Probe (node -e against moderniser/src/node/verdict.js): checkpoint with auth_delta=true, auth_coverage.lost=false, attestations.auth_equivalence=null, all other conjuncts green → {"verdict":"GREEN","reasons":[]}. Trace: auth_delta computed at node/invariants.js:47, consumed nowhere in src (grep); cli.js:154 hard-nulls auth_equivalence ("until its consumer is wired (build-step 7)"); the escalation join (cli.js:115) filters kind==="PARITY_REVIEW" only, so even a decided AUTH_EQUIVALENCE/ATTEST row never reaches a verdict; renderVerdict (sched/loop.js:192) and runComplete (loop.js:204) consult no escalations. verdict.js header self-documents auth as "the one fail-OPEN conjunct". Tests: verdict.test.js covers auth_coverage.lost only; no test asserts auth_delta→attestation-before-GREEN. Rule 5 does not save it: the deferral is ratified in design build-order step 7, §

### F21 [MEDIUM] Durable-state contract drift: design §6.3/§6.5/§3.3 promise per-node .claude/state/nodes/<canonical_sig>.json records carrying schema_version/artifacts/parity/ratchet/attestations; built store is a single runs/<run_id>.state.json without schema_version, and the design was never updated
- Subsystem: docs-contracts
- File: docs/MODERNISER_DESIGN.md:624
- Evidence: §6.3: "// .claude/state/nodes/<canonical_sig>.json ... schema_version: 1.0.0 // resume fails-closed (INDETERMINATE) on unknown major" and §6.5 flow (line 659): "→ .claude/state/plan/<run_id>.plan.json + .claude/state/nodes/*.json →". Built: cli-io.js:17 statePath = join(io.stateDir, "runs", `${runId}.state.json`); loop.js initRun returns {plan_hash, indegree, status, cycle, verdict_green, deferral_track, park_register, activate_mutex, cancel_token} — no schema_version, no per-node artifacts/parity/ratchet/attestations record anywhere.
- Absence-of-guard proof: Grep for "nodes/" as a state path across moderniser/src: no writer or reader exists; loadRun checks plan_hash binding but no state-file schema_version major (the fail-closed-on-unknown-major promise holds only for plan.json). SKILL.md Outputs documents the built runs/ layout, so the two design sections and the skill/code now describe different state stores — the exact "two divergent schemas" defect L9 was locked to prevent.
- Proposed fix: Update §6.3/§6.5 to record the built layout (single run-state file + plan_hash binding) as the offline reducer store, define where per-artifact node-state will live when the DEV-gated reconcile/checkpoint lands, and add schema_version + a fail-closed major check to the run-state file to honour the L9 clause.
- Confidence: CONFIRMED
- Test evidence: moderniser/test/cli.test.js exercises runs/<run_id>.state.json round-trips (passing); no test references .claude/state/nodes/ — passing tests pin the layout the design does not describe.
- Adversarial verification: SURVIVED — Confirmed on every element. Design §6.3 (MODERNISER_DESIGN.md:624-634) promises per-node .claude/state/nodes/<canonical_sig>.json with schema_version 1.0.0 + "resume fails-closed on unknown major" + artifacts/parity/ratchet/attestations/attempts/evidence_refs; §6.5:659 routes the flow through .claude/state/nodes/*.json; L9 (line 78) points to §6.3 as the full contract. Built: cli-io.js:17 writes a single runs/<run_id>.state.json; loop.js initRun (lines 41-51) returns {plan_hash, indegree, status, cycle, verdict_green, deferral_track, park_register, activate_mutex, cancel_token} with no schema_version; grep across moderniser/ finds no nodes/ state-path writer or reader; the schema_version-major fail-closed check exists only in loadPlan (plan.js:120-122) — loadRun checks plan_hash binding only, so the promised state-file version guard is absent. SKILL.md:85 documents the built runs/ layout

### F22 [MEDIUM] Exception family only partially wired: cycle-gate proposeSeams/applyResolution, gate-ui renderPacket, and seam-memory.json persistence have no executable path, and decide cannot carry the CycleResolution payload — while the handoff declares 'Human-by-exception is complete and executable'
- Subsystem: docs-contracts
- File: docs/SESSION_HANDOFF.md:19
- Evidence: Handoff §0: "Exception-module family DONE + Rule-11-reviewed + remediated + WIRED (...cycle-gate...gate-ui...) ... Human-by-exception is complete and executable." But cli-escalations.js:12 imports only recordDecision; no CLI verb calls proposeSeams/applyResolution/lookupLearnedSeam/renderPacket; cli-io.js has no seam-memory read/write; cmdDecide accepts a bare DECISION token (e.g. CUT) with no --edge/--members payload, so cycle-gate's REQUIRED_FIELD validation (cycle-gate.js:42-46) is unreachable from the CLI and no CUT edge is ever recorded. Design §3.4#6 still lists seam-memory.json and quarantine.json state files.
- Absence-of-guard proof: Grep across moderniser/src for cycle-gate/proposeSeams/renderPacket callers outside tests: only the cli-escalations recordDecision import exists. A BREAK_CYCLE decision therefore records WHICH option was chosen but not its content (which edge to cut), and the §3.4#4 promise "resolved seams are learned ... auto-proposes the prior CUT" cannot occur across runs because nothing persists the memory.
- Proposed fix: Either add the wiring (a propose-seams verb over plan break_gate nodes; payload flags on decide for CUT/COGEN/SPROUT; seam-memory.json read/write in cli-io; packet rendering for the surfaced list) or downgrade the handoff claim to 'pure modules built, CLI wiring pending' and update design §3.4#6 to record the deliberate quarantine.json→deferral_track mapping (currently recorded only in escalation-bus.js's header).
- Confidence: CONFIRMED
- Test evidence: moderniser/test/cycle-gate.test.js exercises the pure module directly (passing); moderniser/test/cli.test.js has no cycle-gate/seam-memory coverage — no test exercises the claimed end-to-end 'executable' path because none exists.
- Adversarial verification: SURVIVED — Confirmed by full call-chain trace: cli-escalations.js:12 imports only recordDecision; grep across moderniser/src shows proposeSeams/applyResolution/lookupLearnedSeam/renderPacket have zero production callers (only definitions, tests, and the feedback.js header comment — minFeedbackArcSet's sole caller is the uncalled proposeSeams). cmdDecide (cli-escalations.js:51-66) takes a bare positional DECISION token validated by string membership (gate-ui.js:65 DECISIONS[kind].includes), so 'CUT' passes with no edge and cycle-gate.js:42-46 REQUIRED_FIELD validation is unreachable from any executable path. cli-io.js persists only escalations.json/park-register.json — no seam-memory.json read/write, so the design §3.4#4 cross-run auto-propose promise (MODERNISER_DESIGN.md:410) and §3.4#8 'BREAK_CYCLE→CycleResolution' payload contract (line 439) cannot occur, while §3.4#6 line 422 still lists seam-m

### F23 [MEDIUM] Master architecture spec asserts the moderniser is UNBUILT ('no dir exists', /modernise UNBUILT, controls not wired) while this branch builds all of it — merging ships a master doc that contradicts the tree
- Subsystem: docs-contracts
- File: docs/HARNESS_ARCHITECTURE_SPEC.md:185
- Evidence: "> No `moderniser/` or forge dir exists yet. This is **the next build**" (line 185); build-state table line 30: "| `moderniser/` + forge | *(no dir)* | **UNBUILT** |"; line 245: "**`/feature` and `/modernise` are UNBUILT** `[pending]` ... The `--breakpoint / --audit / --online / --offline / --resume` controls are design-level, not yet wired."; line 62 still tags the moderniser "9-phase" although MODERNISER_DESIGN §6.8 claims the arch-spec tag "is reconciled to match" (only §6 line 193's '6 stages' was reconciled).
- Absence-of-guard proof: The branch end-state contains moderniser/src (26 modules, 272 tests), .claude/skills/modernise/SKILL.md, and wired --online/--breakpoint/--resume at the skill+CLI level; nothing in the spec marks these sections as point-in-time snapshots other than the 2026-07-10 date, and the spec self-describes as the re-baselined-to-reality master.
- Proposed fix: Re-baseline the arch-spec before merge: flip the build-state rows for moderniser//modernise to BUILT-offline (DEV-gated remainder), update §6's banner and §9's STATUS, and fix the §0 table's '9-phase' cell to the 6-stage per-node quality loop per MODERNISER_DESIGN §6.8.
- Confidence: CONFIRMED
- Test evidence: n/a (doc claim) — verified by directory listing, SKILL.md presence, and npm run test:moderniser (272/272 pass) on this branch.
- Adversarial verification: SURVIVED — Finding CONFIRMED by direct probes. (1) All cited spec lines verified verbatim: HARNESS_ARCHITECTURE_SPEC.md:30 table row 'moderniser/ + forge | *(no dir)* | UNBUILT', :185 'No moderniser/ or forge dir exists yet. This is the next build', :245 '/feature and /modernise are UNBUILT [pending] ... controls are design-level, not yet wired', :62 residual '9-phase' tag. (2) Branch end-state contradicts all of it: moderniser/src exists (28 modules), node --test moderniser/test → 272/272 pass (ran live), .claude/skills/modernise/SKILL.md wires --online/--breakpoint/--resume at skill+CLI level (lines 4, 23-25, 32, 38, 47). (3) Snapshot defence fails: the doc self-describes as 'Authoritative build + design reference', is named '(master)' by MODERNISER_DESIGN.md:12 and SESSION_HANDOFF.md:72, and its table header 're-verified against the code, not recalled' is false at merge time. (4) No ratified dec

### F24 [MEDIUM] SESSION_HANDOFF is internally contradictory and every §1 're-verified at write-time' number is stale: §0 vs §1 disagree (90 vs 87 ahead, 815 vs 804 tests, 215 vs 204 moderniser, HEAD 1c7c480 vs 8c2d73c) and reality is 101 ahead / 872/872 / 272/272 / HEAD b91ad32
- Subsystem: docs-contracts
- File: docs/SESSION_HANDOFF.md:28
- Evidence: §1: "git rev-list --count main..HEAD → **87**" and "git log --oneline -1 → 8c2d73c" and "npm test → **804/804**", while §0 line 22 says "HEAD `1c7c480`, **90 ahead of main**, **815/815** (moderniser 215/215)". Re-run now: 101 ahead, HEAD b91ad32, npm test 872/872, moderniser 272/272. Also §0 line 3 lists the exception-module family as REMAINING while §0 line 19 declares it DONE; §5.A says the /modernise entry skill is 'NEXT' while §0 line 16 says it is DONE; §6 step 2 tells the next session to 'expect 8c2d73c / 804/804'.
- Absence-of-guard proof: No section supersedes another explicitly — a fresh session following §6's resume checklist fails its state verification at step 2 and §5.A directs it to build an already-built skill; the operator's git rule ('numbers must be re-verified, never recalled') is violated by a section literally titled 're-verified at write-time'.
- Proposed fix: Regenerate the handoff at HEAD: one consistent §0/§1 number set (101 ahead, b91ad32, 872/872, moderniser 272/272), delete the stale §5.A/§6 instructions, and reconcile the §0 line-3 'remaining' list with lines 16-21.
- Confidence: CONFIRMED
- Test evidence: Probes re-run this session: git rev-list --count main..HEAD → 101; git log -1 → b91ad32; npm test → 872 pass / 0 fail; npm run test:moderniser → 272 pass / 0 fail.
- Adversarial verification: SURVIVED — Confirmed on every axis. Probes re-run this session: git rev-list --count main..HEAD = 101, HEAD = b91ad32 (branch feature/analyser-enrichment), npm test = 872/872, npm run test:moderniser = 272/272 — matching the reviewer's 'reality' and contradicting both §0:22 (1c7c480/90/815/215) and §1:27-30 (8c2d73c/87/804/204). All four internal contradictions verified by direct read: §0:22 vs §1 numbers disagree; line 3 lists the exception-module family as remaining while line 19 declares it DONE+WIRED (and line 21 says everything offline-buildable is built); §5.A (line 81) marks /modernise 'NEXT' while line 16 says DONE/INVOCABLE; §6 step 2 (line 91) tells the next session to expect 8c2d73c/804. Absence-of-guard proven: HEAD commit b91ad32 edits only one §0 bullet yet its own commit message records the true numbers (872/872, moderniser 272/272), so the author had fresh numbers and left §1/§5/§6 

### F25 [MEDIUM] A findings doc without modernization_plan (schema-valid — the ADT-only analyser mode emits exactly that) yields a zero-node plan that status immediately reports complete:true — the factory fails open on schema drift or truncated input
- Subsystem: cross-cutting-seams
- File: moderniser/src/cli.js:50
- Evidence: Probe against a scratch state dir: `plan findings-empty.json` (doc with graph/findings but no modernization_plan) → {run_id, nodes: [], waves: 0} exit 0; `status` → {"complete": true, "counts": {}}. Root: node/scope.js:36 `(doc.modernization_plan?.objects ?? [])` tolerates the absent section; cmdPlan (cli.js:47-64) has no node-count or section-presence guard; runComplete over zero nodes is vacuously true. The drift input is real, not hypothetical: analyser/src/modes.js:134 docFromAdtOnly returns a subset doc with NO modernization_plan, and .claude/schemas/analyser-findings.schema.json requires only [source_system, package, generated_at, findings, s4_readiness, graph] — so a legitimate analyser artifact at the canonical specs/brownfield/analyser-findings.json path drives /modernise (skill exit condition step 5: 'status reports complete: true') to declare the modernisation complete having done nothing.
- Absence-of-guard proof: Traced the whole ingest chain: cmdPlan → assemblePlan → scopeNodes; assemblePlan fails closed only on a plan object MISSING FROM THE GRAPH (assemble.js:49) — the inverse drift (section absent entirely) passes every layer. The skill's prerequisite checks only file existence ('Missing → run /abap-analyser first'); analyser/src/validate-findings.js is never invoked by the moderniser or the skill; no layer distinguishes 'clean package, nothing to modernise' from 'section absent'.
- Proposed fix: In cmdPlan, fail closed when doc.modernization_plan?.objects is not an array (absent/mistyped section → error naming the expected producer mode), and when it is present-but-empty return an explicit {nodes: 0, note: 'nothing to modernise'} rather than letting the generic complete:true stand; alternatively validate the doc against analyser-findings.schema.json plus a moderniser-required-sections check at the plan boundary.
- Confidence: CONFIRMED
- Test evidence: No test covers a doc lacking modernization_plan — every moderniser test uses the full ZFICO fixture or hand-built plans (grep across moderniser/test/: all modernization_plan references assume presence); the passing suite is silent on this path.
- Adversarial verification: SURVIVED — CONFIRMED by re-running the probe and tracing every layer. Probe (scratch --state-dir/--runs-dir): schema-valid doc with graph/findings but no modernization_plan -> `plan` = {nodes: [], waves: 0} exit 0; `status` = {complete: true, counts: {}} exit 0; also `next` = [] and `sweep-order` = {remaining: []}, so the offline default exit terminates instantly too. Mechanism verified: scope.js:36 `doc.modernization_plan?.objects ?? []` tolerates absence; cmdPlan (cli.js:47-64) has no presence/count guard; canonicalNodes (plan.js:143) accepts empty; runComplete (loop.js:204-207) is vacuously true over zero nodes. Guard chain: assemble.js:49 fails closed only on the inverse drift; the /modernise skill checks file existence only (SKILL.md:30); the moderniser never validates the doc. One wording correction that strengthens the finding: validateFindings IS invoked on the analyser side (orchestrator.j

### F26 [MEDIUM] L7 park-register lifecycle is incomplete: a re-park after re-entry is silently unaudited (parkAudit early-returns on the stale row), and the documented successor-probe re-check (reEnter) has no executable caller — the audit file and live state diverge permanently
- Subsystem: cross-cutting-seams
- File: moderniser/src/cli.js:126
- Evidence: parkAudit (cli.js:124-137): `if (reg.parked.some((p) => p.node_id === sig)) return;` — and no CLI path ever REMOVES a row (grep: park.js reEnter has zero non-test callers). End-to-end probe on the ZFICO fixture: park SCR (signed first.signer, probe I_FOO) → register row written; progress PENDING (re-entry) → register untouched (by design, state cleared); re-dispatch, BLOCK NO_RELEASED_SUCCESSOR, PARK again with --signed-by SECOND.SIGNER --justification 'successor I_FOO does not cover cross-client' → park-register.json still shows ONLY first.signer/'no successor v1'/I_FOO, while the live state.park_register carries the second row. The skill (SKILL.md:69) and park.js:44-47 both document 'the probe re-checks each run' / 're-checked each run, §3.4 #5' — no CLI verb, skill step, or loop call performs it.
- Absence-of-guard proof: Traced all writers/readers of park-register.json: only cmdOutcome→parkAudit writes, nothing deletes or reconciles; reEnter (the module built for exactly this per design build-step 6 'park + successor re-probe') is exported but unreferenced outside its own unit test. The idempotence check that protects against crash-retry double-parking is keyed on bare node_id presence, so it cannot distinguish a retry of the SAME park from a genuinely NEW park after re-entry — and because no removal path exists, the stale row guards forever.
- Proposed fix: Wire the lifecycle: (a) on PARK→PENDING re-entry (cmdProgress) or via a new `reprobe` CLI verb calling reEnter with the cloudification-registry availability set, remove/close the audited row (e.g. add released_at rather than deleting, preserving history in-file); (b) make parkAudit's idempotence key include the park generation (e.g. skip only when an OPEN row for node_id exists), so a re-park appends a new audited row with its own signer/justification.
- Confidence: CONFIRMED
- Test evidence: cli.test.js:245 asserts only the FIRST park writes the register (passes); park.test.js:40 asserts reEnter's pure behaviour (passes) — but no test exercises re-park-after-re-entry or any reEnter caller, so both tests pass while the composed lifecycle is broken: the tests assert the units, not the seam.
- Adversarial verification: SURVIVED — End-to-end probe (real CLI subprocess, ZFICO fixture, scratch --state-dir) reproduces the exact divergence: park #1 (first.signer/I_FOO) writes the register; progress PENDING re-enters (state cleared, file kept per loop.js:110-113); re-dispatch → BLOCK → park #2 (SECOND.SIGNER/I_BAR) → park-register.json still shows ONLY first.signer/I_FOO while state.park_register carries SECOND.SIGNER — parkAudit (cli.js:126) early-returns on the stale row, the state row shape lacks successor_probe, and log.jsonl records signed_by but not justification/probe, so episode 2's probe I_BAR is persisted nowhere. Repo-wide grep confirms reEnter has zero non-test callers and no CLI verb/skill step performs the documented per-run successor re-check (SKILL.md:69, park.js:44, MODERNISER_DESIGN.md:413), and no removal/reconcile path exists for the file. Not a ratified deferral: the codebase annotates real deferra

### F27 [LOW] Stage-4 contract drift: co-tenant conflict `groups` are never collapsed at plan-freeze despite the ratified design and conflict.js's own docstring
- Subsystem: moderniser-sched
- File: moderniser/src/sched/assemble.js:54
- Evidence: MODERNISER_DESIGN §3.1 Stage 4: 'The co-tenant `groups` are collapsed into one ordered (expand→contract) super-node **at plan-freeze**'; conflict.js:21-24 docstring repeats it ('Plan-freeze collapses each into one ordered (expand→contract) scheduling super-node (L4)'). assemblePlan consumes only `buildConflictGraph(...).keysOf` (line 128) — `groups` is dead in the scheduler path (grep confirms no consumer) — and the golden fixture's GL/SCR/TOP pool cluster remains 3 separate plan nodes serialized round-by-round by the frontier (spine.test.js:79-91).
- Absence-of-guard proof: No collapse code exists in assemble.js or anywhere in sched/; the doc's promised freeze-time assert ('the freeze asserts every conflict key-space id is a super-node id') is also absent (construction happens to guarantee it). Consequence of no-collapse: co-tenant pool members activate at three separate checkpoints, so a half-modernised program pool is live between checkpoints — the exact state L4's collapse+ordering was specified to prevent; the mutex only serializes activation, it does not make the cluster move as one ordered unit. Note the doc is internally two-minded (the same paragraph says cluster members 'may still co-generate'), so this may resolve as a doc amendment rather than a code change.
- Proposed fix: Either implement the ratified collapse (consume conflict `groups` at assemble, merging each co-tenant cluster into one plan node with migration-safety-supplied intra-order) or amend MODERNISER_DESIGN §3.1 Stage 4 and the conflict.js docstring to ratify the implemented keys+frontier-serialization+mutex semantics and delete/repurpose the dead `groups` output. Surface to the operator for the ratification call.
- Confidence: LIKELY
- Test evidence: spine.test.js:60-92 and frontier.test.js:71-77 assert the co-tenants schedule as SEPARATE serialized nodes — the tests ratify the semantics that contradict the design sentence, so either the doc or the tests assert the wrong contract.
- Adversarial verification: SURVIVED — Factual core CONFIRMED: MODERNISER_DESIGN.md:123 and conflict.js:21-25 both state groups are collapsed into one ordered super-node at plan-freeze; assemble.js:128 consumes only keysOf, grep shows .groups has zero production consumers, freezePlan (plan.js) has neither collapse nor the promised key-space assert, and spine.test.js:79-91/frontier.test.js:71-77/assemble.test.js:79 (all 42 tests pass, re-run) pin the GL/SCR/TOP cluster as 3 separate serialized plan nodes — real drift, since commit 227a337 and the handoff declare stages 1-6 BUILT. But the severity rationale is refuted: L4's ratified hazard (design line 73) is the SE11/E070 enqueue race UNDER PARALLEL WRITES ('one write lost, or a half-modernised pool activates'), which the built frontier direct-conflict exclusion + per-transport activate mutex fully mitigate; the between-checkpoint mixed-pool state is inherent to the ratified p

## Unverified LOW findings (pass-through, not adversarially verified)

- [LOW/analyser-and-deps] topoWaves recurses over the UNCAPPED object list — renderHtml throws RangeError (stack overflow) on ~10k-deep dependency chains, crashing the CLI --html run
  File: analyser/src/html-graph.js:138 · Confidence: CONFIRMED
  Evidence: Line 138: const wave = topoWaves(og.objects, og.adj); runs over ALL objects BEFORE the MATRIX_CAP filter (line 143-146). topoWaves.compute (lines 226-234) recurses once per link of the longest dependency chain. Probe: a linear chain of 10,000 objects -> 'RangeError: Maximum call stack size exceeded' with stack frames 'at compute (html-graph.js:227/231)'; 5,000-object chain renders fine in 30ms. The two sibling recursions are cap-bounded (stronglyConnected is called with the capped <=120 objects 
  Proposed fix: Rewrite topoWaves iteratively (Kahn-style wave peeling over in-degree, or an explicit-stack DFS), matching the depth-bounding discipline the docstrings already claim for the other traversals. ~10 lines, deterministic result unchanged.

- [LOW/analyser-and-deps] renderHtml fragment mode is consumer-less and untested speculative surface — and it silently hands the CSP obligation to a host that has no helper to compute the required hash
  File: analyser/src/html-report.js:41 · Confidence: CONFIRMED
  Evidence: if (opts.fragment) return inner; — documented as 'host supplies skeleton + CSP'. Repo-wide grep: renderHtml is consumed only by analyser/cli.js:85 (no opts) and the test file; 'fragment: true' / 'fragment' appears in no .js, no .md, no skill, no MCP server. No test in html-report.test.js exercises fragment mode. A future host that does embed the fragment must independently compute sha256(APP_JS) for its own CSP or every report control dies — an undocumented, unexported seam (the host must import
  Proposed fix: Either delete the opts parameter and the early return until a real embedding host lands, or wire the actual consumer (e.g. the /analyze skill's in-session artifact render) plus a test in the same change, and export a helper (e.g. appJsCspHash()) so hosts cannot get the hash seam wrong.

- [LOW/moderniser-state-ratchet] canonicalJSON silently serializes non-plain objects (Date/Map/Set) as "{}" — hash-degeneracy in the determinism primitive, inconsistent with its own throw-on-non-JSON posture
  File: moderniser/src/state/canonical-json.js:29 · Confidence: CONFIRMED
  Evidence: Probe: `canonicalJSON(new Date("2026-01-01"))` -> `"{}"` (JSON.stringify gives `"2026-01-01T00:00:00.000Z"`); `canonicalJSON({t:new Date(1)}) === canonicalJSON({t:new Date(2)})` -> true (two distinct values, one hash); Map and Set likewise collapse to "{}". The function deliberately throws on functions, symbols, bare undefined, and non-finite numbers (lines 38, 43) — a fail-closed posture for non-JSON inputs — but the object branch (line 29) enumerates own keys with no plain-object/toJSON check,
  Proposed fix: In write(), before enumerating keys: reject non-plain objects fail-closed — `const proto = Object.getPrototypeOf(v); if (!Array.isArray(v) && proto !== Object.prototype && proto !== null) throw new Error("canonicalJSON: non-plain object");` (or explicitly honour toJSON first, then document it). One 

- [LOW/moderniser-graph] stripComment treats an INDENTED '*' line as a full-line comment, but ABAP comments require '*' in column 1 — an arithmetic continuation line beginning with '*' is code and is erased before the seal/edge scan (under-approximation)
  File: moderniser/src/graph/augment.js:100 · Confidence: CONFIRMED
  Evidence: Line 100: `if (/^\s*\*/.test(line)) return "";` with comment 'no valid statement begins with *'. Probe: source "lv = lv_a\n   * lo_obj->(lv_meth)." -> node NOT sealed, although the second line is legal ABAP code (multiplication continuation; only a column-1 '*' makes a comment line) and contains a dynamic method call that SEAL_PATTERNS[1] would otherwise catch.
  Proposed fix: Change the full-line-comment test to /^\*/ (column-1 only). Strictly safer: it still strips every genuine ABAP comment line (which must start at column 1) and can never erase code. One-character diff; add a regression test with an indented-'*' continuation line carrying a dynamic construct.

- [LOW/moderniser-graph] RESOLVABLE patterns fire at most once per line (regex .exec without /g): the second of two chained resolvable statements on one physical line silently loses its synthetic edge
  File: moderniser/src/graph/augment.js:81 · Confidence: CONFIRMED
  Evidence: scanNode line 81: `const m = re.exec(line); if (!m) continue;` — one match per pattern per line. Probe: source "PERFORM f1 ON COMMIT. PERFORM f2 ON COMMIT." (one line, legal ABAP statement chaining) yields synthetic edges [F1] only — F2's edge is silently absent, no seal compensates.
  Proposed fix: Use matchAll with a /g/i version of each RESOLVABLE regex (or loop re.exec on a sticky regex) so every occurrence on the line emits an edge. Two-line change; add a test with two PERFORM ... ON COMMIT statements on one line asserting two edges.

- [LOW/moderniser-graph] RAW_SEAL pattern \bENHANCEMENT\b seals a node on any prose comment containing the English word 'enhancement' — right fail-direction, but a throughput amplifier that can park large fractions of a real brownfield corpus behind spurious human seam-confirmations
  File: moderniser/src/graph/augment.js:45 · Confidence: CONFIRMED
  Evidence: Line 45: `const RAW_SEAL_PATTERNS = [/\bENHANCEMENT\b|\bENHO\b|\bENHSPOT\b/i];` scanned on every RAW line (line 77), comments included by design. A comment like '* enhancement request 4711: rebate logic' seals the whole node as NEEDS_MANUAL_SEAM, making it undispatchable (frontier.js:54) until a human confirms a seam that does not exist. 'Enhancement' is common English in SAP comments, so real corpora will over-seal at scale.
  Proposed fix: Tighten to the marker shapes the design actually names, e.g. /^\s*\*?\s*ENHANCEMENT(?:-POINT|-SECTION)?\s+\S|\bENHANCEMENT\s+\d|"\{\s*Begin\s+ENHO\b|\bENHSPOT\b/i (statement-form ENHANCEMENT n id., ENHANCEMENT-POINT/SECTION, abapGit ENHO braces, ENHSPOT), keeping the raw-line scan. Validate against 

- [LOW/moderniser-sched] loadPlan's tamper check does not cover the stored `waves` array (or carried fields) — a tampered plan loads as 'verified'
  File: moderniser/src/sched/plan.js:124 · Confidence: CONFIRMED
  Evidence: loadPlan verifies `plan.plan_hash !== planHash(plan.nodes)`, but planHash RE-DERIVES waves from nodes, so the stored `waves` field is never compared. Probe: after savePlan, rewrite the file with `waves: [['n2'],['n1']]` (inverted), `session_budget: 999999`, `edges_ref: [['evil','edge']]` → "TAMPERED PLAN ACCEPTED. waves: [[\"n2\"],[\"n1\"]] budget: 999999 edges_ref: [[\"evil\",\"edge\"]]".
  Proposed fix: In loadPlan, recompute waves from the verified nodes and deep-compare to the stored `plan.waves` (throw on mismatch), or overwrite the returned artifact's waves with the derived value so no consumer can read a tampered copy.

- [LOW/moderniser-sched] freezePlan accepts duplicate node ids; the frontier then emits the same sig twice in one batch
  File: moderniser/src/sched/plan.js:157 · Confidence: CONFIRMED
  Evidence: canonicalNodes validates id shape and wave but not uniqueness. Probe: `freezePlan({nodes:[{id:'dup',wave:0},{id:'dup',wave:3}]})` → "duplicate-id plan accepted; nodes: 2"; `initRun` silently collapses to 1 status entry and `nextDispatch` returns `["dup","dup"]` — a frontier batch violating its own 'both-graph independent' contract (dispatch then throws only on the second GROUNDED→GROUNDED, far from the cause).
  Proposed fix: In canonicalNodes, after sorting, throw on adjacent equal ids: `plan: duplicate node id '<id>'`.

- [LOW/moderniser-node-loop] PARK defect-guard treats malformed-but-present evidence as absent — contradicts its own 'any PRESENT failing conjunct forces BLOCK' comment
  File: moderniser/src/node/verdict.js:57 · Confidence: CONFIRMED
  Evidence: (typeof cp.atc_p1 === "number" && cp.atc_p1 > 0) || cp.unit?.green === false || …
Probe: nodeVerdict({ block_reason: 'NO_RELEASED_SUCCESSOR', atc_p1: '5' }, {}) → PARK; unit:{green:'false'} → PARK; invariants:{intact:0} → PARK — string/loose-typed failing evidence in the checkpoint file slips the hasDefect guard (lines 54-60) while the same values would BLOCK the GREEN path (strict !== 0 / !== true checks).
  Proposed fix: In hasDefect, treat present-but-not-well-typed evidence as a defect: e.g. (cp.atc_p1 !== undefined && cp.atc_p1 !== 0), (cp.unit !== undefined && cp.unit.green !== true), (cp.invariants !== undefined && cp.invariants.intact !== true) — 'absent' means the KEY is absent, not that the value fails a typ

- [LOW/moderniser-node-loop] parity data-source transformations trigger expects successor_kind tokens ('table'/'cds') no in-repo producer emits — unpinned seam vocabulary
  File: moderniser/src/node/parity.js:50 · Confidence: LIKELY
  Evidence: if (t.kind === "released-api" && ["table", "cds"].includes(t.successor_kind)) classes.add("data-source");
Fixture/analyser probe: the analyser's only successor-kind vocabulary is TADIR-style — blast_radius.successor_kind = "CDS_STOB" (blast-radius-report.js:44, c.successors[0]?.type); plan transformations carry released_successor as a NAME ("I_GLACCOUNTINCOMPANYCODE"), no kind field at all.
  Proposed fix: Pin the seam: either enumerate the accepted tokens in the @param and normalise inside classifyParity (e.g. map CDS_STOB/DDLS/STOB→'cds', TABL→'table'), or change the trigger to accept the analyser's actual successor-kind vocabulary. One-line normaliser plus a test using 'CDS_STOB'.

- [LOW/cli-skill-exception] cli.test.js throws-assertions are reason-vacuous — the `|Command failed` alternative (plus the echoed command line) matches any failure, so refusal-reason regexes verify nothing
  File: moderniser/test/cli.test.js:84 · Confidence: CONFIRMED
  Evidence: `assert.throws(() => cli("outcome", rid, sig, "GREEN"), /verdict|Command failed/i)`. Probe: execFileSync's error message for an entirely unrelated failure (unknown run id) is "Command failed: C:\\Program Files\\nodejs\\node.exe …cli.js outcome no-such-run x GREEN …" — /verdict|Command failed/i matches it (and the echoed argv makes even the first alternative match trivially whenever the command word appears in the invocation). Every throws-assertion in the file carries this alternative.
  Proposed fix: Assert on the attached stderr instead: catch the error and `assert.match(String(e.stderr), /no recorded green verdict/)` (execFileSync attaches stderr to the thrown error); drop the `|Command failed` alternative.

- [LOW/cli-skill-exception] SKILL.md offline exit condition wedges a literal reader when any node machine-BLOCKs offline, and the GATED→GENERATED checkpoint retry is never instructed
  File: .claude/skills/modernise/SKILL.md:48 · Confidence: CONFIRMED
  Evidence: Step 5 offline exit: "when `next` returns `[]` and every dispatched node sits at `SYNTAX_OK`, the gated pass is done" — after a SYNTAX_CEILING `outcome BLOCK` (step 3's own instruction), the blocked node sits at BLOCK, so the conjunct is never true and a literal session has no defined path to the draft sweep. Step 4 instructs `outcome <sig> GREEN|BLOCK|…` after a red verdict without mentioning the FSM's GATED→GENERATED retry (legal while cycle < 3, per node-status.js) — a fresh session quarantin
  Proposed fix: Reword the exit condition to "when `next` returns `[]` and no dispatched node can advance (nodes at SYNTAX_OK or terminal BLOCK/PARK)", and add to step 4: "on a red verdict with cycle < 3, retry via `progress <sig> GENERATED` (the FSM counts the cycle and voids the stale verdict); BLOCK only at the 

- [LOW/docs-contracts] §3.2 #4 promises the SELF_CHECK retry loop 'persisted even on FAILED so a mid-cycle crash resumes at the failing cycle, not cycle 1' — but failed syntax attempts are unpersistable in the built FSM and SKILL.md delegates the 3-cycle count to the orchestrating agent's memory
  File: docs/MODERNISER_DESIGN.md:177 · Confidence: CONFIRMED
  Evidence: Design §3.2 #4: "Fail loops back to TRANSFORM (bounded 3–5 cycles; loop state persisted even on FAILED so a mid-cycle crash resumes at the failing cycle, not cycle 1)." SKILL.md:43: "On a fail, regenerate WITHOUT reporting SYNTAX_OK and count the failed attempt yourself (the FSM's cycle counter only sees SYNTAX_OK→GENERATED / GATED→GENERATED retries)". node-status.js:71 increments cycle only on those two transitions; GENERATED→GENERATED is illegal, so no CLI call can record a failed syntax attem
  Proposed fix: Either add a persistable failed-attempt counter (e.g. a progress verb that records a syntax failure without a status move, feeding the same cycle map), or amend §3.2 #4 to record prose-counted syntax retries as the accepted offline compromise, keeping the persisted guarantee for checkpoint (GATED→GE

- [LOW/docs-contracts] MODERNISER_DESIGN residual staleness bundle: 'does not exist yet' status header; 'all-TypeScript controller' vs built .js ESM (ratified only in the handoff); unbuilt modernise.manifest.yaml whose escalation list omits PARITY_REVIEW (6 vs 7 kinds); §1/§3.1 'graph_json' vs actual envelope key 'graph'; §6.5 '--breakpoint <phase>' vs wave semantics; §6.3 canonicalJSON '\n endings' not emitted; §6.6 log-row fields (retry_count/tokens/wall_ms) not written
  File: docs/MODERNISER_DESIGN.md:3 · Confidence: CONFIRMED
  Evidence: Line 3: "moderniser/ does not exist yet; this document is its build spec"; §3.5/§5#1: "the controller is all-TS" (built: .js+JSDoc, ratified in SESSION_HANDOFF §3#4 but never propagated); §3.5 manifest sketch escalation list = 6 kinds vs §3.4/escalation-bus.js = 7 (PARITY_REVIEW missing) and moderniser/modernise.manifest.yaml does not exist; §1/§3.1 say input is "graph_json {nodes[], edges[]}" while the fixture/orchestrator emit top-level `graph` (§6.2 has it right); §6.5 says "--breakpoint <pha
  Proposed fix: One doc-sweep commit: fix the status header, replace 'all-TS' with the ratified .js+JSDoc convention, either build the manifest or mark it pending and add PARITY_REVIEW to its sketch, s/graph_json/graph/ in §1/§3.1, s/<phase>/<wave>/ in §6.5, drop the '\n endings' clause, and align §6.6's row schema

- [LOW/cross-cutting-seams] cmdVerdict commits state (verdict_green) before the baseline pair — the crash residue between the two writes is fail-OPEN (GREEN earnable with ceilings never established), contradicting cli-io's own 'residual tear is fail-SAFE by monotonicity' doctrine
  File: moderniser/src/cli.js:160 · Confidence: CONFIRMED
  Evidence: cli.js:160 `saveState(io, runId, recordVerdict(plan, state, sig, r));` then cli.js:161-164 `if (r.green && flags.record !== undefined) { … writeBaselinePair(…) }`. A crash between the two leaves verdict_green[sig]=true durably committed with per_object[sig] never written; `outcome GREEN` then succeeds (loop.js:130 checks only verdict_green) and the node goes terminal with its warn ceiling still seed-∞ and no coverage floor — the next modernisation of that object gets a free establish-pass. cli-i
  Proposed fix: Swap the order: writeBaselinePair first, saveState(recordVerdict) second — the crash residue then becomes advanced-baselines-without-verdict, which is over-blocking (fail-safe) and idempotently healed by the retry, matching the module's stated doctrine. One-line reorder plus a comment; optionally ex

## Per-subsystem verdicts (finder summaries)

### analyser-and-deps
The CSP hardening is genuinely sound: probes confirm the emitted inline script is byte-identical to APP_JS, the CSP sha256 hash matches the extracted emitted bytes, APP_JS is pure ASCII with no </script or <!-- parser hazards, the findings JSON island unicode-escapes < > & so hostile payloads cannot close it or inject a second executable script (exactly 2 script tags, one inert), there are zero inline on*= handlers and zero eval/innerHTML/href surfaces, and style-src 'unsafe-inline' covers only width:N% bars and display:none toggles (a documented, test-ratified residual). The @abaplint/core pin is consistent (exact 2.119.53 in package.json and lockfile, single entry, integrity present) and all 398 analyser tests pass. Three residuals survive: the branch's load-bearing CSP test hashes the imported APP_JS constant instead of the bytes extracted from the rendered HTML (tautological — a script-tag wrapping regression would silently kill all report interactivity with the suite green); the DSM's recursive topoWaves runs over the uncapped object list and provably blows the stack at ~10k-deep dependency chains (pathological input, CONFIRMED crash, LOW realism); and renderHtml's fragment mode has zero consumers and zero tests (speculative surface contradicting the no-dead-code standard).

### moderniser-state-ratchet
The moderniser state primitives are in strong shape: the node-status FSM implements the ratified §6.3 edge list exactly (fail-closed on unknown/illegal edges, cycle-capped retry loop, reason-gated BLOCK→PARK, GREEN terminal, escalation from precisely the seven active states), canonicalNodeId is fail-closed with a real delimiter-collision guard, canonical-json handles cycles/-0/undefined/key-order/DAG-sharing correctly, and the ratchet's core semantics — seed-∞ establish-then-tighten, only-down warn delta on changed lines, only-up coverage with floor fallback, signed atc_warn_delta coherently consumed by nodeVerdict via renderVerdict, and onPass copy-on-write that provably never moves baselines on BLOCK — are all correct, probe-verified, and pinned by 272/272 passing tests including a cross-module coherence test. The three survivors are all in the fail-closed evidence claim the module makes for itself: (HIGH) the diff side of the warn join fails open — missing or malformed diff_changed_lines yields a silent delta-0 PASS and establishes the baseline at 0 (cli.js's `?? []` erases absence, and the CLI test fixture itself rides through the hole), directly contradicting the docstring's 'Missing evidence fails CLOSED' and §3.2 5.3; (MEDIUM) a corrupt atc-baseline ceiling (null/string) or corrupt coverage_floor_pct silently resets/waives the ratchet while the exactly-analogous covBaseline entry corruption is guarded with 'baseline-corrupt'; (LOW) canonicalJSON silently hashes Date/Map/Set to "{}" (two distinct Dates hash identically) where the rest of the module throws on non-JSON inputs — mitigated in the plan path only by loadPlan's re-hash throwing later. All three are mechanical, few-line fixes that tighten the gate in the direction the design already ratified.

### moderniser-graph
The moderniser graph subsystem's core algorithms are sound and were verified adversarially, not just by the (green, 272/272) suite: the iterative Tarjan in condense.js matched a reference recursive implementation on 400 randomized graphs and always produced a DAG on 200 dense ones (plus 20k-chain/130k-ring scale runs); elsOrder/backEdges/minFeedbackArcSet in feedback.js held every load-bearing property on 750 randomized probes — total deterministic arrangement, back-edge removal always yields a DAG (the FAS property), greedy seam loop terminates with every residual sub-component within budget, reported sub-components byte-match the real post-cut condensation, seams are always real input edges, and confidence is honestly 0-with-structural-marker on no-shrink cuts (the Math.max spread-ceiling claim in its comment was also verified live: RangeError at 130k args); conflict.js's linear keysOf/buckets/groups representation, star-union, and program_pools[]/transports[] super-node keying match §3.1 Stage 4 exactly and never materialise the O(k²) clique; build.js's precedenceEdges orientation (dependency→dependent, ratified 2026-07-11) is consistent across the design doc, kahnLevels' in-degree semantics, assemble's wiring, and the fixture test that pins the entry report as the precedence sink; the CLI's unwired augment/transport/session-budget options are recorded deferrals (SESSION_HANDOFF.md §B), not gaps. All five surviving findings are in augment.js's scan surface — the one module where the design forbids under-approximation: two MEDIUMs (a probed set of unscanned dynamic constructs — write-DML `UPDATE/INSERT/MODIFY/DELETE (var)`, `CALL/LEAVE TO TRANSACTION <var>`, `CREATE OBJECT … TYPE (var)`, kernel `GET/CALL BADI`, `PERFORM form(prog) ON COMMIT` — none in the doc's known-limits register; and a confirmed seam defect where augment's emitted target tokens match neither node-id space so dangling synthetic edges — including cycle-closing ones — are silently dropped by condense inside assemblePlan with no error, while the same function fail-closes on other missing ids) plus three LOWs (indented-`*` lines wrongly stripped as comments vs ABAP's column-1 rule, first-match-only RESOLVABLE scanning losing the second chained statement on a line, and the \\bENHANCEMENT\\b raw-scan sealing nodes on prose comments — safe direction, real human-review throughput cost).

### moderniser-sched
The moderniser scheduler is a strong subsystem: the ratified design decisions (bottom-up precedence orientation, seed-∞ establish-then-tighten ratchet, wave-move-or-drop REPLAN gate, shared 3-cycle retry budget, keys-not-clique conflict representation) are implemented faithfully; plan freeze/hash/load, Kahn levels with a byte-stable indegree seed, transitive in-plan dependency reduction through out-of-plan nodes (verified exactly-once counter decrements across diamonds), and the L7 park path are all fail-closed and covered by a green 272-test suite. The survivors cluster at channel seams the FSM alone cannot guard: (1) HIGH — applyProgress (and the shipped CLI's `progress` verb) accepts terminal statuses, minting an unearned GREEN without a verdict while desyncing the indegree counter (dependents starve forever) and skipping quarantine/mutex release — a one-word prose slip defeats the central earned-GREEN GAN invariant, proven end-to-end through the production CLI against a scratch state dir; (2) the stale-verdict clear fires only on retry edges, so a green verdict recorded before a PARK/NEEDS_MANUAL_SEAM exit blesses the regenerated artifact after re-entry; (3) dispatch re-checks readiness and park defensively but not the plan-level dynamic_seal, so the L5 human-seam gate is frontier-only; (4) the augment→assemble seam is shape-incompatible with overApproximateEdges' actual node-level output — seals and cycle-closing synthetic edges silently drop (under-approximation, the exact direction L5 forbids) and the production CLI never wires augment at all; (5) plan-freeze never performs the design-ratified co-tenant group collapse (conflict `groups` is dead code; tests ratify the contradicting semantics — needs an operator ratification call); plus two LOW fail-closed gaps (loadPlan's tamper check misses the stored waves/carried fields; freezePlan accepts duplicate node ids). All findings except the doc-drift item are probe-confirmed; none are covered by the existing tests, which uniformly exercise only the guarded channel.

### moderniser-node-loop
The moderniser per-node gate subsystem (verdict/invariants/parity/scope, 272/272 tests green) is largely sound where it was attacked hardest: every GREEN conjunct fails closed on missing/malformed evidence (probed), auth is fail-open exactly and only as ratified (L7 proven-loss), the PARITY_REVIEW attestation branch is forgery-proof and temporally bound (register-only join, run+cycle stamps at decide time, re-raise/regeneration voids — verified via cli.test.js), vetoes and scope_reduced are never attestable (probed), parity bands are half-open and correct at both 0.30/0.70 boundaries including FP rounding (all reachable scores are 0.05-multiples), and scope's driving-rule canonical_sig is finding-order-stable, pipe-fail-closed, with correct blast dedup and order-independent min-root union-find pools. The survivors cluster in invariantDiff's auth-coverage model, which is asymmetric on both sides of the diff: a before-side DCL restriction that vanishes in the after is never counted as coverage loss (fail-open straight through the verdict's deliberately fail-open auth conjunct — the one path where auth can silently weaken to GREEN), while an unchanged pre-existing WITH PRIVILEGED ACCESS entry is re-counted as a fresh loss on every cycle (permanent false BLOCK, internally inconsistent with auth_delta=false). Adjacent to that, auth_delta has zero consumers and the verdict has no auth-attestation conjunct — a documented build-step-7 deferral, but its interim direction is fail-open rather than fail-closed. Two smaller items: the PARK defect-guard treats malformed-present evidence as absent (mitigated by the downstream named-sign-off gate), and the parity transformations trigger expects successor_kind tokens ("table"/"cds") that the only in-repo successor-kind producer never emits ("CDS_STOB") — an unpinned seam vocabulary for the future extractor. GREEN online paths remain DEV-credential-gated and human-released, which caps all severities below CRITICAL.

### cli-skill-exception
The /modernise shell is genuinely hard to defeat on its advertised paths — run-id containment, durable idempotent commits, verdict-at-GATED gating with --record baseline movement, sweep-ledger isolation, the escalation lifecycle (idempotent raise, typed decisions, correct receipt after re-raise, latest-row voiding, cross-run refusal), the documented GATED→GENERATED retry voiding, and P8 log scrubbing all held under real subprocess probes, and every CLI verb/flag the SKILL references exists. But the branch's two headline guarantees — earned GREEN and the attestation temporal binding — are both defeated by the FSM's re-entry paths (PARK→PENDING and NEEDS_MANUAL_SEAM→PENDING): regeneration after re-entry goes GROUNDED→GENERATED, which neither voids verdict_green nor increments the cycle counter, so a never-verdicted regenerated artifact can be marked GREEN (probe-confirmed) and a stale or even pre-artifact PARITY_REVIEW attestation blesses code the human never saw (probe-confirmed). Both share one fix locus: treat every entry into GENERATED as a new artifact generation. Below that sit three confirmed medium items (sweep-order hides failed dep drafts from the generator; a re-park silently keeps the first park's audit row and successor probe; the SKILL promises a park re-probe and GatePacket rendering that no command implements — reEnter/renderPacket/levelDisposition/clusterOscillations have zero production callers) and two low items (reason-vacuous throws-assertions in cli.test.js; SKILL offline exit-condition and retry-instruction gaps, both failing in the safe direction).

### docs-contracts
The built moderniser core is faithful to the load-bearing contracts: §3.2's GREEN conjunction (incl. the ratified needs_review-attestation branch), §6.1's parity weights/bands/vetoes and register-only temporally-bound attestation join, §6.3's plan_hash object-wrapper + fail-closed loadPlan + status FSM, §6.4's seed-infinity signed-delta ratchet, and the §3.4 7-kind taxonomy all match code exactly, with 272/272 moderniser tests (872/872 full suite) green and every implemented CLI verb documented in SKILL.md. The drift that survives is in the docs layer, in both directions: docs promising what code does not do (L7 auth-equivalence attestation "before PASS" is unenforced and hard-nulled; §6.3/§6.5's per-node .claude/state/nodes/*.json store does not exist; L1's full RAP surface incl. .dcls is absent from the frozen artifact skeleton; seam-memory/cycle-gate/gate-packet have no executable path) and docs asserting pre-build states (the master arch-spec says moderniser is UNBUILT with "no dir exists"; the handoff contradicts itself across sections and all its "re-verified" numbers are stale — actual: 101 commits ahead, HEAD b91ad32, 872/872). None of these are runtime defects today (the affected paths are DEV-cred-gated), but merging as-is ships a master spec and a resume handoff that misdescribe the tree, and two security-adjacent contracts (auth attestation, .dcls reconciliation surface) whose enforcement currently rests on prose.

### cross-cutting-seams (finder summary)
The moderniser spine itself held up under attack: the reducer is genuinely fail-closed (FSM-guarded moves, plan_hash binding on every entry point, earned-GREEN refusal, retry-edge verdict clearing, temporally-bound PARITY_REVIEW attestations — all re-verified by probe), the CLI has zero SAP-write/child-process/network surface so P4/P5/P8 cannot be violated from this code, sig-space vs object-space joins are coherent everywhere I traced (plan ids/baselines/escalations in sig-space; members/member_meta/frontier risk in object-space, keyed consistently), and the TODO-shaped docstring debts (augment source extraction, build-step-7 auth/currency-matrix, manifest-pinned thresholds) are each genuinely recorded in MODERNISER_DESIGN.md/SESSION_HANDOFF.md, so they are ratified deferrals, not gaps. All 272 moderniser tests pass. The survivors are exactly at the cross-subsystem file seams the code cannot see: (1) the branch made the moderniser CLI a second writer of .claude/state/{atc,abapunit}-baseline.json while the first writer's contract (abap-evaluator.md) and the files' own _note fields still claim sole evaluator ownership in a per_object-less schema — an evaluator write in its documented shape silently resets every ratchet ceiling to seed-∞ (probed: BLOCK→PASS), and the design's claimed "node lock" for concurrent sessions does not exist anywhere in the repo; (2) a schema-valid subset findings doc (docFromAdtOnly emits no modernization_plan; the schema does not require it) produces a zero-node plan that `status` immediately reports complete:true — the factory declares victory on drifted input (probed); (3) the L7 park audit register misses re-parks after re-entry (probed: second signer/justification never lands) and the documented successor-probe re-check (reEnter) has no executable caller; (4) cmdVerdict commits state (verdict_green) before the baseline pair, a two-write seam whose crash residue is fail-open, contradicting cli-io's own fail-safe-tear doctrine — narrow trigger, ranked LOW honestly.

---

## Remediation addendum — 2026-07-13

All Phases A–C+E of the remediation work order (docs/SESSION_HANDOFF.md §4) executed; every fix
was TDD'd RED-first and then INDEPENDENTLY verified by a 23-agent Rule-11 workflow (fresh-context
adversarial verifiers re-running THIS report's probes against the fixed HEAD; 22 completed, L3's
verifier was re-verified inline). The verifiers confirmed 10/12 probed fix clusters closed on the
first pass and CAUGHT THREE ESCAPES the fixes had missed — all three closed in `959e7a0` and
covered by new RED-first tests. Suite at addendum time: **902/902** (moderniser 302/302 within it).

### Disposition table (F1–F27 + L1–L15)

| Finding | Disposition | Commit(s) | Independent verification |
|---|---|---|---|
| F1 [HIGH] ratchet diff fail-open | FIXED | 885c74a | ALL-FIXED (29 probes) |
| F2 [HIGH] applyProgress terminal bypass | FIXED | 98d2b25 | ALL-FIXED (23 probes incl. CLI) |
| F3 [HIGH] re-entry stale verdict | FIXED | 98d2b25 | ALL-FIXED (two-layer void proven unavoidable by FSM enumeration) |
| F4 [HIGH] attestation binding | FIXED + escape closed | 98d2b25, 959e7a0 | probes B/C closed; verifier caught the `plan --force` same-run-id leak → run_epoch binding added |
| F5 [HIGH] baseline second writer | FIXED | d8167eb | contract propagation verified on all 4 surfaces |
| F6 [MED] analyser CSP test tautology | **OPERATOR CONSULT** (analyser no-touch) | — | — |
| F7 [MED] corrupt ceiling/floor fail-open | FIXED | 885c74a | ALL-FIXED |
| F8 [MED] seal taxonomy round 2 | FIXED + escapes closed | 407d37f, 959e7a0 | 9 constructs seal; verifier caught INSERT INTO (var) + PERFORM…IN PROGRAM…ON COMMIT → 3 patterns added |
| F9 [MED] synthetic-edge token space | **DEFERRED → Phase D5** (augmentFromCpg adapter, operator decision) | — | — |
| F10 [MED] stale verdict over PARK/seam re-entry | FIXED (with F3) | 98d2b25 | ALL-FIXED incl. production `reprobe` path |
| F11 [MED] dispatch seal veto | FIXED + escape closed | e30519e, 959e7a0 | dispatch veto held; verifier caught `progress GROUNDED` bypass → applyProgress refuses GROUNDED, applyOutcome refuses non-terminals |
| F12 [MED] augment→assemble shape no-op | **DEFERRED → Phase D5** | — | — |
| F13 [MED] removed DCL invisible to loss | FIXED | 858be7a | ALL-FIXED (13 probes) |
| F14 [MED] privileged re-count | FIXED | 858be7a | ALL-FIXED |
| F15 [MED] auth_delta unconsumed | **DEFERRED → Phase D1** (wire auth attestation) | — | — |
| F16 [MED] sweep swept/failed conflation | FIXED | e30519e | confirmed + PENDING-only guard |
| F17 [MED] re-park audit row dropped | FIXED | e30519e | park cluster ALL-FIXED |
| F18 [MED] SKILL unwired promises | FIXED (option a) | 60a0523 | `reprobe`/`packets` verbs confirmed working end-to-end |
| F19 [MED] L1 artifact skeleton surface | **DEFERRED → Phase D2** | — | — |
| F20 [MED] auth attestation doc-promised | **DEFERRED → Phase D1** | — | — |
| F21 [MED] per-node state-file drift | **DEFERRED → Phase D3** (amend design to single-file) | — | — |
| F22 [MED] exception family partial wiring | PARTIALLY FIXED (packets/reprobe); rest → **Phase D4** | 60a0523 | — |
| F23 [MED] spec asserts moderniser UNBUILT | FIXED + leftovers closed | cf1a0c9, a213259 | verifier confirmed the 4 cited surfaces, caught 2 leftover lines → fixed |
| F24 [MED] contradictory old handoff | SUPERSEDED | 566cb62 | — |
| F25 [MED] zero-node plan vacuously complete | FIXED | e30519e | confirmed (missing section + empty list both fail loud) |
| F26 [MED] park lifecycle incomplete | FIXED | e30519e, 60a0523 | confirmed (replace-not-skip + reprobe re-entry) |
| F27 [LOW] conflict groups not collapsed | **OPERATOR CONSULT** (ratify code semantics vs implement collapse) | — | verifier: severity rationale refuted (mutex+frontier mitigate); drift real |
| L1 topoWaves recursion (analyser) | CONFIRMED — **OPERATOR CONSULT** | — | verified: RangeError at ~10k chain |
| L2 fragment mode (analyser) | CONFIRMED — **OPERATOR CONSULT** | — | verified: consumer-less |
| L3 canonicalJSON non-plain "{}" | FIXED (verified inline — verifier died on session limit) | 0300e80 | inline probe: Date/Map→"{}", distinct dates hashed equal |
| L4 stripComment indented '*' | FIXED | 1cfce13 | CONFIRMED; verifier corrected the probe vector (legal ABAP) |
| L5 RESOLVABLE once per line | FIXED | 1cfce13 | CONFIRMED |
| L6 ENHANCEMENT prose over-seal | FIXED | 1cfce13 | CONFIRMED; verifier corrected the artifact's regex (still matched prose) |
| L7 loadPlan waves tamper | FIXED | 0300e80 | CONFIRMED; stored-waves-into-hash form per verifier |
| L8 freezePlan duplicate ids | FIXED | 0300e80 | CONFIRMED |
| L9 PARK malformed evidence | FIXED | 06db436 | CONFIRMED; verifier extended probe list (auth/warn-delta) — covered |
| L10 parity successor_kind seam | FIXED (pinned + normaliser) | 06db436 | CONFIRMED; name-only contract shape recorded as extractor debt |
| L11 vacuous throws-assertions | FIXED (throwsWith on stderr, 14 sites) | 4f061b8 | CONFIRMED |
| L12 SKILL offline exit wedge | FIXED | a213259 | CONFIRMED; exit covers BLOCK/PARK/NMS; retry instruction added |
| L13 §3.2#4 persisted-FAILED claim | FIXED (doc amendment + docstring) | 06db436, a213259 | CONFIRMED |
| L14 design residual staleness bundle | FIXED | a213259 | CONFIRMED; verifier's per-item corrections applied |
| L15 verdict-before-baselines tear | FIXED (verifier-corrected order) | 4f061b8 | CONFIRMED; GATED-guard-first shape preserves the no-out-of-lifecycle invariant |

### Verifier-noted residuals (recorded, not defects)
- Quoted-literal dynamic parens (`UPDATE ('ZTAB')`) and colon-chained statements (`CALL TRANSACTION: lv…`) are
  statically resolvable / rare shapes outside the seal scan — candidates for the design's known-limits register.
- F25's guard lives in cmdPlan; the library API (savePlan/saveState) could still persist a zero-node run —
  unreachable through the CLI today.
- Frozen-plan `dynamic_seal` never clears, so a human-confirmed seam re-enters via REPLAN only — undocumented
  (relevant to Phase D4's cycle-gate wiring).

### Open for the operator (Phase D + analyser consult)
D1 auth-attestation wiring (F15/F20) · D2 L1 full artifact surface (F19) · D3 single-file state design
amendment (F21) · D4 cycle-gate verbs (F22 rest) · D5 augmentFromCpg adapter + token normalisation (F9/F12) ·
F27 conflict-collapse ratification · analyser items (F6 CSP test, L1 topoWaves, L2 fragment mode) ·
`/analyze`→`/analyse` rename.
